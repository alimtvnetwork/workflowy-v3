# Task Counter — No-Questions Mode

**Status:** 🟢 ACTIVE
**Activated:** 2026-04-27
**Tasks completed: 31 / 40**

---

## Log

| # | Date | Task summary | Ambiguities logged |
|---|------|--------------|--------------------|
| 01 | 2026-04-27 | Spec consolidation pass (F1+F2+F3): bumped 7 parent SSOTs with addendum cross-refs, refreshed 99-consistency-report.md to v2.2.0 (inventory 18→27), added §8 footnote to round-4 audit linking B1–B4 work, updated spec-index.md version cells. | 1 (`01-audit-already-closed.md`) |
| 02 | 2026-04-27 | Registered 50 new ATs (`AT-APP-58..107`) from B1–B4 addendums in canonical `spec/31-app/97-acceptance-criteria.md` (v2.6.0); added 8 dispatch rows + validation history to `spec/31-app/01-features/97-acceptance-criteria.md` (v2.2.0). | 1 (`02-at-prefix-mgp-vs-mpg.md`) |
| 03 | 2026-04-27 | F4 SQL DDL audit: added `ReaperRuns` table (B4/11b) to `02-app-schema.sql` v2.1.0; added `IdxItem_UpdatedAt`, `IdxItem_LiveByUpdatedAt` (partial), `IdxReaperRuns_RanAt`, plus commented FTS5 template to `03-app-indexes.sql` v2.1.0; added Naming Bridge section to sql/`00-overview.md` v2.1.0 documenting `Item↔Items`, `Content↔Title`, `FractionalIndex↔SortOrder` aliases. | 1 (`03-ddl-naming-bridge.md`) |
| 04 | 2026-04-27 | F6 endpoint contracts: created 4 endpoint files — `11b-trash-reaper.md` (EP-REAPER-RUN, EP-REAPER-RUNS-LIST), `15-search.md` (EP-SEARCH-QUERY), `14b-sync-replay.md` (EP-SYNC-REPLAY), `09b-mirror-peer-group.md` (EP-MIRRORS-GROUP-GET, EP-MIRRORS-DETACH). Bumped `16-endpoint-at-matrix.md` v1.0.0→v1.1.0 (41→47 endpoints, 6 new rows, coverage counts updated). Added addendum-files note to `00-overview.md`. | 1 (`04-at-prefix-for-new-endpoints.md`) |
| 05 | 2026-04-27 | F7 ERD refresh: bumped `03-app-db-erd.md` v1.0.0→v1.1.0 — added `MirrorPeerGroup`, `MirrorPeerGroupMember`, `ReaperRuns` entities + relationships; deprecated legacy `Mirror` (kept for back-compat). Bumped `06-indexes.md` v1.0.0→v1.1.0 — added 4 new indexes (`IdxItem_UpdatedAt`, `IdxItem_LiveByUpdatedAt` partial, `IdxReaperRuns_RanAt`, `IdxMirrorPeerGroupMember_ItemId` UNIQUE partial, `IdxMirrorPeerGroupMember_GroupId`); added 4 endpoints + edges to flowchart; reversed prior "NOT needed" stance on `IdxItem_UpdatedAt`. | 1 (`05-mirror-table-deprecation.md`) |
| 06 | 2026-04-27 | F8 G-29 gate: created algorithm SSOT `22-g29-endpoint-matrix-coverage-gate.md` v1.0.0; shipped runner `scripts/spec-hygiene/29-check-endpoint-matrix-coverage.mjs` (148 lines, exit 0/1/2, 4-axis check: orphan-endpoint, phantom-row, missing-at-citation, duplicate-row); registered G-29 in `02-ci-quality-gates.md` and `scripts/spec-hygiene/00-run-all.mjs`. Gate caught + fixed pre-existing matrix drift (3 ID renames + 1 missing `EP-TRASH-PURGE-ALL` row). Final state: ✅ 48 declared, 48 matrixed, all paired with ≥1 AT. | 1 (`06-matrix-prexisting-drift.md`) |
| 07 | 2026-04-27 | F9 AT citation fix: discovered + fixed 7 wrong AT-APP citations in 4 endpoint files created in F6 (numbers off by 3–10 from canonical registry). Fixed `11b-trash-reaper.md` (78→81..84, 81→85), `14b-sync-replay.md` (90..94→97..102), `15-search.md` (100..103→103..107), `09b-mirror-peer-group.md` (`AT-MGP-58..62` invented → `AT-APP-58,60,65` + `AT-APP-61,63,64`). Replaced bad `AT-APP-24, AT-TRASH-07` for `EP-TRASH-PURGE-ALL` with correct `AT-APP-19, AT-TRASH-08, AT-TRASH-09`. G-29 still ✅ green. | 1 (`07-at-citation-drift.md`) |
| 08 | 2026-04-27 | F10 G-30 gate: created algorithm SSOT `23-g30-at-citation-validity-gate.md` v1.0.0; shipped runner `scripts/spec-hygiene/30-check-at-citation-validity.mjs` (190 lines) supporting three declaration shapes — single-ID (backticked or plain), backticked range (`AT-APPF-01..05` enumeration), and open-prefix placeholder (`AT-INFO-NN` series licensing). Caught + fixed two regex bugs during dev (digit-swallowing prefix, missed un-backticked first cells). Registered in `02-ci-quality-gates.md` row G-30 + `00-run-all.mjs`. Gate is green: 856 closed IDs + 32 open prefixes covering 163 unique citations across 201 sites. Negative-tested with bogus `AT-BOGUS-99` injection (correctly exits 1). | 1 (`08-at-open-prefix-licensing.md`) |
| 09 | 2026-04-27 | F11 workflow specs: authored 4 cross-feature flow files in `02-workflows/` — `05-trash-reaper-flow.md` (cron→batch→cascade→audit, AT-WF-REAPER-01..05↔AT-APP-81..85), `06-search-query-flow.md` (debounce→permission filter→5-tier scoring→tie-break, AT-WF-SEARCH-01..05↔AT-APP-103..107), `07-sync-replay-flow.md` (online→FIFO drain→ServerTs stamp→LWW→SSE, AT-WF-REPLAY-01..06↔AT-APP-97..102), `08-mirror-detach-flow.md` (membership delete→auto-dissolve trigger→SSE, AT-WF-DETACH-01..05↔AT-APP-60..65 subset). Bumped `00-overview.md` v2.1.0→v2.2.0; added Open-Prefix Declarations table for G-30 licensing. G-29 + G-30 still ✅ green (open prefixes 32→36; 877 closed IDs). | 1 (`09-detach-at-scope.md`) |
| 10 | 2026-04-27 | F12 v1→v2 Mirror Peer-Group migration plan: bumped `07-migrations.md` v1.0.0→v2.0.0; promoted M-115/M-116/M-117 from placeholders to **allocated** slots for the peer-group migration (Phase 1 create / Phase 2 backfill / Phase 3 drop legacy `Mirror`+`MirrorOfItemId`); allocated M-118 for `ReaperRuns`; renumbered Phase-2 placeholders to M-119..M-121. Added §"v1→v2 Mirror Peer-Group Migration — Execution Plan" with pre-flight checks (incl. mandatory file backup, SQLite≥3.35 check), 10-step transactional sequence, 4 failure modes (orphan FK, singleton group, ALTER fail, crash mid-`user_version` bump), forbidden patterns, and rollback procedure. All slot rows link to existing SQL files. G-29 + G-30 still ✅ green. | 1 (`10-migration-numbering-vs-naming-bridge.md`) |
| 11 | 2026-04-27 | F13 feature-slices refresh: bumped `04-feature-slices.md` v1.0.0→v2.0.0. Replaced §4.2 (legacy `Mirror` source/placeholder ERD) with new **Mirror Peer Groups** slice (`MirrorGroup`/`MirrorMember`, ≥2-member invariant, auto-dissolve trigger note, naming-bridge footnote). Bumped §4.6 from "Trash (a query)" → "Trash + Reaper Audit": added second ERD for `ReaperRuns` (audit-only, no FKs), updated flowchart with reaper→audit edge, added `IdxReaperRuns_RanAt` note. Added two new slices: §4.10 Search (no new tables; flowchart for permission-filter→tier-scoring path; touched tables Item/Share/WorkspaceMember/Tag/ItemTag) and §4.11 Sync Replay (no new tables; reuses SyncCursor; flowchart for online→drain→LWW→SSE; notes ProcessedMutations not yet allocated). All sections now carry `**ATs**:` cross-refs. G-29 + G-30 still ✅ green. | 1 (`11-feature-slices-not-in-g30-scope.md`) |
| 12 | 2026-04-27 | F14 G-30.1 scope extension: bumped runner `30-check-at-citation-validity.mjs` to v1.1.0 — added `CONSUMER_SCOPES` array now scanning `02-workflows/` (recursive) and `07-db-diagram/04-feature-slices.md` (single-file filter) in addition to original `06-endpoints/`. Failure output now prefixes each violation with `[scope-label]` for provenance. Bumped SSOT `23-g30-at-citation-validity-gate.md` v1.0.0→v1.1.0 (replaced single-bullet scope with 3-row table; restated "out of scope" with rationale; added v1.1.0 row to change history). Updated G-30 row in `02-ci-quality-gates.md` to enumerate the 3 consumer paths. Gate green on first run: 877 closed IDs + 36 open prefixes covering 282 citations (was 175) across 3 scopes — F11 workflows + F13 feature-slices both citation-clean by construction, no fixes needed. | 1 (`12-g30-scope-extension-boundaries.md`) |
| 13 | 2026-04-27 | F15 closed two open AT prefixes: bumped `01-features/97-acceptance-criteria.md` v2.2.0→v2.3.0. Added new section "Inline-Prefix Alias Enumeration (closed)" with two tables — `AT-INFO-01..07` ↔ `AT-INFOMODEL-01..07` (7 rows, used by EP-ITEMS-LIST/GET/ROOT/CREATE/UPDATE/TURN-INTO) and `AT-MIRROR-01..06` ↔ `AT-MIRRORS-01..06` (6 rows, used by EP-MIRRORS-CREATE/LIST/DELETE). 13 new closed registrations (G-30 closed IDs 877→890). Open-prefix declarations retained in Coverage Map for naming-convention documentation but no longer the only declaration source for these 13 IDs. Added v2.3.0 row to validation history. G-29 + G-30 still ✅ green (open prefixes 36→38 because the new alias section literally includes the strings `AT-INFO-NN`/`AT-MIRROR-NN` as prose — harmless coexistence). | 1 (`13-alias-vs-open-prefix-coexistence.md`) |
| 14 | 2026-04-27 | F16 mirror-create flow: created `02-workflows/09-mirror-create-flow.md` v1.0.0 (~190 lines) following the F11 sequence-pinning template (Why → Actors → Preconditions → Sequence → Failure modes → Idempotency → Forbidden patterns → Acceptance Tests → Related). 6-step transactional create sequence pinned: auth × 2 → liveness × 2 → SQL recursive-CTE cycle check → group lookup-or-create with `FOR UPDATE` → child Item INSERT → dual `MirrorPeerGroupMembers` INSERT → COMMIT → SSE `mirrors.created`. New `AT-WF-CREATE-NN` open-prefix licensed for canonical-mapped subset `AT-APP-58, 59, 62, 66, 67` (5 ATs; non-overlapping with F11's detach subset). Bumped `02-workflows/00-overview.md` v2.2.0→v2.3.0 — added file to dispatch table, added namespace row to canonical-map table, added v2.3.0 extension paragraph. G-29 + G-30 still ✅ green (closed IDs 890→895; open prefixes 38→39; citations 282→297). | 1 (`14-mirror-create-at-subset.md`) |
| 15 | 2026-04-27 | F17 migration-execution flow: created `02-workflows/10-migration-execution-flow.md` v1.0.0 (~250 lines) — operational runbook complementing the SQL-detail SSOT at `07-db-diagram/07-migrations.md`. Pinned 7-step bootstrap orchestration with global lock + serial per-workspace iteration: detect → SQLite-version pre-flight → snapshot workspace list → per-DB (cron lock + backup + 10-step DDL via `07-migrations.md` + audit row) → aggregate report → conditional `workflowy_schema_version=2` bump → batch-complete SSE. Documented HTTP 503 (mid-migration mirror writes) and HTTP 410 (post-migration legacy-table writes) runtime contracts. Added Observability section enumerating 6 mandatory log events tagged with `MigrationBatchId`. New `AT-WF-MIGRATE-NN` open-prefix licensed for canonical-mapped subset `AT-APP-66, 67` (6 acceptance tests covering happy-path, idempotent-replay, legacy-410, partial-batch failure, audit-late recovery, singleton sweep). Bumped `02-workflows/00-overview.md` v2.3.0→v2.4.0. G-29 + G-30 still ✅ green (closed IDs 895→901; open prefixes 39→40; citations 297→313). | 1 (`15-migration-flow-vs-sql-ssot-split.md`) |
| 16 | 2026-04-27 | F18 ERD↔SQL naming-bridge reconciliation: bumped `sql/00-overview.md` v2.1.0→v2.2.0 — split bridge into 3 subsections (Tables & columns, Indexes, Reverse pointers); added 4 new column-level rows (PK/FK aliases for MirrorGroup/MirrorMember) and 3 new index-name rows (`IdxMirrorMember_*`/`IdxMirrorGroup_*` ↔ `IdxMirrorPeerGroupMember_*`/`IdxMirrorPeerGroup_*`). Added back-pointer `## Naming bridge` footnotes to `03-app-db-erd.md` v1.1.0→v1.2.0 and `06-indexes.md` v1.1.0→v1.2.0 (one paragraph each, link-only — no duplication). `04-feature-slices.md` + `07-migrations.md` already linked correctly from F13/F12; unchanged. SSOT location decision (Option B — keep inside `sql/00-overview.md`) logged in `16-naming-bridge-ssot-location.md`. G-29 + G-30 still ✅ green (citations stable at 313 — naming bridge is prose-only, no AT references). | 1 (`16-naming-bridge-ssot-location.md`) |
| 17 | 2026-04-27 | F19 Favorites slice: bumped `04-feature-slices.md` v2.0.0→v2.1.0 — added §4.12 Favorites slice (~55 lines) for the `Favorite` table that's been DDL-allocated since `02-app-schema.sql` v1.0.0 but had no per-feature slice. Slice covers: ERD diagram (Item ||--o{ Favorite), 4 invariants (UNIQUE(UserId,ItemId), FractionalIndex NOT NULL, ON DELETE CASCADE on hard-delete only, logical FK to Root.User), wire-path flowchart (click → EP-ITEMS-UPDATE{isFavorited} → INSERT/DELETE Favorite → sidebar render via IdxFavorite_UserId_FractionalIndex), touched-tables list, current-MVP endpoints (`EP-ITEMS-UPDATE`), explicit "deferred — do NOT invent EP-FAV-*" guard, sidebar-reorder note, ATs (`AT-LAYOUT-12` + open prefix `AT-LAYOUT-NN`), outstanding-contradiction callout linking to ambiguity log #17. Discovered + logged a stale claim in `06-endpoints/03-layout-structure.md` line 20 ("no favorites table in MVP") that contradicts v1.0.0 DDL — deliberately deferred to F23 to keep F19 atomic and out of endpoint files. G-29 + G-30 still ✅ green (closed IDs 901→901; open prefixes 40→40; citations 313→315 — `AT-LAYOUT-12` already known, only 2 new sites added). | 1 (`17-favorites-endpoint-vs-table-contradiction.md`) |
| 18 | 2026-04-27 | F20 close 12 more open prefixes: bumped `01-features/97-acceptance-criteria.md` v2.3.0→v2.4.0 — added §"Inline-Prefix Closure (identity + alias, 12 prefixes, 67 IDs)" with single consolidated table (vs F15's per-prefix tables — justified by 11 identity prefixes vs F15's renamed aliases). 67 closure rows: 11 identity prefixes (`AT-BOARD/CONCURRENCY/CTXMENU/INTERACT/LAYOUT/PAGE/ROLES/SHARE/TEMPLATES/TODAY/TRASH-NN`, 60 rows) + 1 alias closure (`AT-MULTI-NN` → `AT-MULTISELECT-NN`, 7 rows). Each row: closed alias ID → source ID → source file link → consumer scope. Validated all 67 IDs exist in source files via Python script before listing. Added v2.4.0 row to validation history. G-29 still ✅ green; G-30 closed IDs 901→908 (+7, not +67 — 60 IDs were already registered via source-file AT tables; this is correct + logged in ambiguity #18 as a discovery about how G-30 walks all of spec/31-app/, not just the dispatch index). Open prefixes 40→41 (+1, harmless `AT-MULTI-NN` mention in prose). Citations stable at 315. | 1 (`18-closure-vs-source-registration.md`) |
| 19 | 2026-04-27 | F21 bidirectional mirror-flow cross-references: bumped `08-mirror-detach-flow.md` v1.0.0→v1.1.0 (added 2 missing links: `09-mirror-create-flow.md` as **inverse path** + `10-migration-execution-flow.md` as bootstrap-time singleton-sweep mimic) and `09-mirror-create-flow.md` v1.0.0→v1.1.0 (sharpened the existing 08 link with explicit "create grows ≥2 / detach shrinks / shared auto-dissolve" semantic + added missing link to `10-migration-execution-flow.md` as the batch-create path via M-116 backfill). `10-migration-execution-flow.md` already correctly cross-referenced both 08 and 09 from F17 — left untouched. Audit found 08 was the only orphan in the mirror-flow trio (08↔09↔10 now fully bidirectional with semantic-rich link descriptions). G-29 + G-30 still ✅ green (no AT changes; declarations + citations stable). | 1 (`19-mirror-flow-xref-asymmetry.md`) |
| 20 | 2026-04-27 | F22 non-Mirror index-name drift audit: bash-diffed all `IdxXxx` identifiers between `sql/03-app-indexes.sql` (24 indexes) and `06-indexes.md` (41 mentions). Found **3 real drift** rows referencing post-M-117 dropped column/table (`IdxItem_MirrorOfItemId`, `IdxMirror_SourceItemId`, `IdxMirror_MirrorItemId`) — deleted from "App DB Required Indexes" table + added one-line v2-deprecation footnote citing M-117. Found **1 miscategorisation**: `IdxUser_Email` listed as required but is only `UNIQUE`-side-effect (no `CREATE INDEX` in `01-root-schema.sql`) — moved to new §"Implicit Indexes (UNIQUE-implied)" subsection naming `sqlite_autoindex_User_*` as the actual backing index. Cleaned up `## Index → Query Map` Mermaid flowchart: removed deprecated nodes I4 (`IdxItem_MirrorOfItemId`) + I5 (`IdxMirror_SourceItemId`); added new node I19 (`IdxMirrorPeerGroupMember_GroupId`); rewired `EP-MIRRORS-LIST` (E5) edge from dropped I5 → live I19. Bumped `06-indexes.md` v1.2.0→v1.3.0. SQL DDL untouched (correct already). New follow-up F26 logged for auditing other `UNIQUE` columns. G-29 + G-30 still ✅ green (no AT changes). | 1 (`20-index-drift-deprecated-rows.md`) |
| 21 | 2026-04-27 | F23 stale Favorites/Pinned shell-endpoint claims: bumped `06-endpoints/03-layout-structure.md` v1.0.0→v1.1.0. Reframed §Summary line 12 from "No server endpoints — UI-only feature" → "No shell-specific endpoints — UI-only feature; persistence reuses items endpoints", with explicit callout that the ⭐ Favorite toggle persists via `EP-ITEMS-UPDATE`. Renamed §"Why no endpoints" → §"Why no shell-specific endpoints"; deleted the stale "no 'favorites' table in MVP" claim (contradicted DDL since v1.0.0 of `02-app-schema.sql` + the F19 §4.12 slice + `01-information-model.md` line 60 + `03-layout-structure.md` line 200) and the speculative "future Pinned flag" sentence (zero hits in `01-information-model.md` or `07-db-diagram/`). Added 2 new cross-refs: to information-model §relationships + feature-slices §4.12 as the canonical Favorites SSOTs, and to `04-information-model.md` for the items endpoint family. G-29 + G-30 still ✅ green (no AT changes; this is prose realignment only). | 1 (`20-favorites-shell-endpoint-stale-claim.md`) |
| 22 | 2026-04-27 | F24 G-30.2 open-prefix redundancy advisory: bumped runner `30-check-at-citation-validity.mjs` v1.1.0→v1.2.0 (added `findRedundantOpenPrefixes()` + `printRedundancyAdvisory()` functions, `--warn-redundant` flag / `G30_WARN_REDUNDANT=1` env-var activation, 5-entry `REDUNDANCY_ALLOWLIST` for future-licensing prefixes `AT-FOO/WORKFLOWS/ROADMAP/ENDPOINTS/DBDIAGRAM-`). Bumped SSOT `23-g30-at-citation-validity-gate.md` v1.1.0→v1.2.0 (added 65-line §G-30.2 section: problem statement, what-it-reports, allow-list table, WARN-only rationale, sample output, invocation patterns, future promotion path; bumped change history). Bumped G-30 row in `02-ci-quality-gates.md` to mention G-30.2 advisory. Probe via `/tmp/probe_g30.mjs` confirmed 36 / 41 open prefixes currently surface as cleanup candidates (5 allow-listed). Default G-30 invocation unchanged (CI-safe); advisory opt-in only. Master runner output unchanged — only the new flag activates §G-30.2. WARN-vs-ERROR design choice logged in `21-g30-redundancy-warn-not-error.md` (chose WARN to avoid defensive re-adding during F3-poly merge work). G-29 + G-30 still ✅ green. | 1 (`21-g30-redundancy-warn-not-error.md`) |
| 23 | 2026-04-27 | F25 cross-flow Related-section asymmetry audit: built `/tmp/audit_xrefs.mjs` 9×9 reciprocity matrix probe; found 6 asymmetric forward-links across the 9 flow files. Triaged each: 5 represented real semantic gaps + 1 was cargo-cult "sister flow" boilerplate. Bumped `04-trash-restore-flow.md` v1.1.0→v1.2.0 (added back-link to `05` describing inverse-window relationship on `Item.DeletedAt`). Bumped `05-trash-reaper-flow.md` v1.0.0→v1.1.0 (DROPPED stale "02 sister flow" forward-link; ADDED 3 back-links: to `07` re HTTP-410 on reaper-deleted rows, to `08` re peer-group cascade trigger reuse, to `10` re M-117 bootstrap singleton-sweep mimic). Bumped `07-sync-replay-flow.md` v1.0.0→v1.1.0 (added back-link to `09` re offline mirror-create FIFO drain). Final reciprocity: 0/0 asymmetric (was 6); per-flow degree map: 02→[03,04], 03→[02,04], 04→[02,03,05], 05→[04,06,07,08,10], 06→[05,07], 07→[05,06,08,09], 08→[05,07,09,10], 09→[07,08,10], 10→[05,08,09]. Deliberately deleted (not added) the 05→02 link to avoid propagating "sister cross-feature flow" boilerplate that would itself need a meaningless back-link. Drift root-cause + future-proofing options logged in `22-cross-flow-link-asymmetries.md`. G-29 + G-30 still ✅ green. | 1 (`22-cross-flow-link-asymmetries.md`) |
| 24 | 2026-04-27 | F26 UNIQUE-column index audit + fabricated-column discovery: built `/tmp/audit_unique.mjs` enumerating all 13 UNIQUE declarations across `01-root-schema.sql` + `02-app-schema.sql`; initial result 3/13 documented. Found **3 critical bugs** + 6 doc gaps. Critical: (1) `06-indexes.md` line 110 claimed `IdxMirrorPeerGroupMember_ItemId` was "UNIQUE partial WHERE DetachedAt IS NULL" but no `DetachedAt` column exists in DDL — detach is row DELETE per `02-workflows/08-mirror-detach-flow.md` step 3c; (2) `03-app-db-erd.md` line 174 fabricated `TEXT DetachedAt` on `MirrorPeerGroupMember` and line 166 fabricated `TEXT DissolvedAt` on `MirrorPeerGroup` (dissolve = row DELETE via trigger, not soft-delete); (3) `IdxUserRole_User_Role` explicitly created in DDL line 97 but undocumented. Bumped `03-app-db-erd.md` v1.2.0→v1.3.0 (deleted both fabricated columns, added missing `CanonicalItemId` FK on MirrorPeerGroup). Bumped `06-indexes.md` v1.3.0→v1.4.0 (corrected partial-WHERE claim → full-table UNIQUE; added `IdxUserRole_User_Role` row; expanded §Implicit Indexes from 1 → 9 entries split into Root DB + App DB sub-tables enumerating sqlite_autoindex_* for all enum/composite UNIQUE declarations). Corrected naming-bridge row in `sql/00-overview.md` line 33. Final audit: 13/13 UNIQUE declarations ✅ documented (was 3/13). G-29 + G-30 still ✅ green. Drift root-cause (B1 design pivot from soft-delete → row-delete trigger; ERD + indexes never updated) + potential G-31 DDL↔doc drift gate logged in `23-fabricated-soft-delete-columns.md`. | 1 (`23-fabricated-soft-delete-columns.md`) |
| 25 | 2026-04-27 | F27 G-30.2 redundancy queue drain (allow-list strategy): inspected all 36 flagged prefixes; discovered 0/36 were vestigial — every row was intentional convention documentation in `01-features/97-acceptance-criteria.md` Coverage Map (kept on purpose by F15 v2.3.0 + F20 v2.4.0 closure notes) or `02-workflows/00-overview.md` §Open-prefix-declarations (canonical-map contract from F11/F16/F17). Chose Option B (expand allow-list) over Option A (delete prose rows) to preserve naming-scheme docs. Bumped runner v1.2.0→v1.3.0 — expanded `REDUNDANCY_ALLOWLIST` from 5 → 41 entries grouped into 3 documented intent-categories: (a) Future-licensing 5 entries, (b) Convention-documentation 14 entries (`AT-INFO/MIRROR/MULTI/BOARD/CONCURRENCY/CTXMENU/INTERACT/LAYOUT/PAGE/ROLES/SHARE/TEMPLATES/TODAY/TRASH-`), (c) Namespace-placeholder 22 entries (source-file inline prefixes `AT-INFOMODEL/MIRRORS/MULTISELECT/DV/SM/MGP/TR/MZ/TPL/OQ/SR-`, all 9 `AT-WF-*` workflow prefixes, plus canonical `AT-APP-` + frozen `AT-APPF-`). Each entry has a one-line rationale comment. Bumped SSOT `23-g30-at-citation-validity-gate.md` v1.2.0→v1.3.0 — replaced 5-row allow-list table with 3-category summary table; added v1.3.0 row to change history; rewrote allow-list paragraph documenting reversibility procedure (delete entry → re-run `--warn-redundant` → optionally delete prose row). Final state: G-30.2 reports `🎉 no cleanup candidates`. Unblocks F28 (promote G-30.2 to default-on) cleanly. Strategy decision + reversibility procedure logged in `23-g30-drain-via-allowlist-vs-deletion.md`. G-29 + G-30 still ✅ green (declarations + citations stable: 908 closed, 41 open, 315 citations, 233 unique). | 1 (`23-g30-drain-via-allowlist-vs-deletion.md`) |
| 26 | 2026-04-27 | F28 G-30.2 promotion to DEFAULT-ON: bumped runner `30-check-at-citation-validity.mjs` v1.3.0→v1.4.0 — flipped `WARN_REDUNDANT` constant from opt-in (`--warn-redundant` / `G30_WARN_REDUNDANT=1`) to default-on with opt-out semantics (`--no-warn-redundant` / `G30_WARN_REDUNDANT=0`). Legacy `--warn-redundant` flag preserved as no-op for back-compat with v1.2.0–v1.3.0 saved CI scripts. Updated runner header docblock with v1.4.0 entry explaining safety rationale (F27 drained queue to 0). Negative-test probe: temporarily commented out `AT-FOO-` allow-list entry → default run surfaced exactly 1 cleanup candidate in stdout while exit code stayed 0; restored cleanly. Bumped SSOT `23-g30-at-citation-validity-gate.md` v1.3.0→v1.4.0 — rewrote §G-30.2 to reflect default-on status (no flag needed); added "Why default-on (v1.4.0)" subsection explaining new-author signal model (close citations OR allow-list with rationale); split sample output into clean-state and drift-detected examples; rewrote §Invocation with `--no-warn-redundant` opt-out + back-compat note; added v1.4.0 row to change history. Bumped G-30 row in `02-ci-quality-gates.md` to mention v1.4.0 default-on + opt-out flag/env var. Master suite confirmed: G-30 still ✅ green; G-30.2 advisory now appears in master output as `🎉 no cleanup candidates` without flag. The 6 pre-existing master failures (numbering/headers/links/shape/contracts/enum) remain unrelated and unchanged. Three future-promotion steps (WARN→ERROR, machine-checkable rationale comments, generated allow-list report) logged but explicitly NOT scoped to F28. CLI design choices (keep legacy flag as no-op, GNU-style `--no-` negation, advisory excluded from master exit-code aggregation) logged in `24-g30-default-on-opt-out-design.md`. G-29 + G-30 still ✅ green; counts stable (908 closed, 41 open, 315 citations, 233 unique). | 1 (`24-g30-default-on-opt-out-design.md`) |
| 27 | 2026-04-27 | F29 G-31 workflow xref reciprocity gate (promoted from F25 prototype): authored `scripts/spec-hygiene/31-check-workflow-xref-reciprocity.mjs` v1.0.0 (~145 lines) — typed exit codes (0/1/2), `ASYMMETRIC_BY_DESIGN` allow-list (string-keyed, empty initially), strict `/^\d{2}-.+-flow\.md$/` filename filter (excludes overview/AT-registry/keyboard-shortcuts), filename-substring link detection over `## Related` section (handles both bare references and markdown links). Registered in `00-run-all.mjs` between G-30 and the index-generator. Wrote SSOT `24-g31-workflow-xref-reciprocity-gate.md` v1.0.0 (~190 lines): why-it-exists (F25 drift class), scope table, 5-step algorithm pseudocode, allow-list semantics + example, output examples (clean + drift), exit-code table, out-of-scope list, "why reciprocity not graph connectivity" justification, future-promotion ladder (G-31a extend scope to other folders, G-31b machine-checkable rationale comments mirroring G-30.3 plan, G-31c documentation-island detection), change history. Added G-31 row to `02-ci-quality-gates.md` registry; updated note to mention G-29/G-30/G-31 all-implemented. Negative-test probe: sed-rewrote `05-trash-reaper-flow` → `XX-broken-link` in `04-trash-restore-flow.md` → runner correctly reported 1 asymmetry with exit 1 + per-violation hint; restored cleanly. Master suite confirms G-31 ✅ green (9 files scanned, 28 cross-flow links, 0 unreciprocated). The same 6 pre-existing master failures (numbering/headers/links/shape/contracts/enum) remain unrelated. Design decisions (reciprocity vs strong-connectivity vs density-floor; strict vs all-md filename filter; string vs object-keyed allow-list; cross-domain references explicitly out of scope) logged in `25-g31-scope-and-invariant-design.md`. G-29 + G-30 + G-31 all ✅ green. | 1 (`25-g31-scope-and-invariant-design.md`) |
| 28 | 2026-04-27 | F30 G-32 DDL UNIQUE documentation coverage gate (promoted from F26 prototype): authored `scripts/spec-hygiene/32-check-ddl-unique-coverage.mjs` v1.0.0 (~210 lines) — parses 3 UNIQUE syntactic forms (column-level `UNIQUE`, table-level `UNIQUE(...)`, explicit `CREATE UNIQUE INDEX`), tracks current `CREATE TABLE` context for column attribution, generates 2-candidate documentation names per non-explicit decl (`Idx{Table}_{Cols}` OR `sqlite_autoindex_{Table}`), substring-matches against `06-indexes.md`, typed exit codes (0/1/2), `COVERAGE_EXEMPT` allow-list with composite key format `${fileBase}:${signature}` (where signature is `Table(Cols)` or `explicit:IndexName`). Registered in `00-run-all.mjs` after G-31. Wrote SSOT `25-g32-ddl-unique-coverage-gate.md` v1.0.0 (~225 lines): why-it-exists (F26 3-bug drift class — fabricated soft-delete columns, undocumented explicit indexes, misclassified UNIQUE-side-effect indexes), scope table, 5-step algorithm pseudocode, allow-list semantics + key-format spec + example, output examples (clean + drift), exit-code table, out-of-scope list (reverse drift, non-UNIQUE indexes, FTS5/triggers, cross-DB FK), "why name presence not structural parity" justification, future-promotion ladder (G-32a reverse-drift sub-check that would have caught F26's fabricated columns at gate level, G-32b non-UNIQUE extension, G-32c machine-checkable rationale comments). Added G-32 row to `02-ci-quality-gates.md` registry; updated note to mention G-29/G-30/G-31/G-32 all-implemented. Negative-test probe: sed-rewrote `IdxUserRole_User_Role` → `IdxXXX_BROKEN` in `06-indexes.md` → runner correctly reported 1 undocumented decl with exit 1 + per-violation source/expected hint; restored cleanly. Master suite confirms G-32 ✅ green (2 files scanned, 13 declarations: 7 column + 4 table + 2 explicit, 0 undocumented). The 6 pre-existing master failures remain unrelated. Cleaned up obsolete `/tmp/audit_unique.mjs` prototype. Design decisions (substring vs table-row vs AST matching strictness; 3-form vs PK-inclusive parsing; 2-candidate naming convention rationale; composite-key allow-list format) logged in `26-g32-matching-strictness-and-key-format.md`. G-29 + G-30 + G-31 + G-32 all ✅ green. | 1 (`26-g32-matching-strictness-and-key-format.md`) |
| 29 | 2026-04-27 | F-future-G32a — promoted G-32 to v2.0.0 by adding **G-32.2 reverse-drift sub-check**. Extended runner (~352 lines): added `collectDocIndexNames()` (regex-extracts every backticked `Idx*`/`sqlite_autoindex_*` from `06-indexes.md`, 37 distinct), `collectDdlIndexNames()` (3 sources — explicit `CREATE [UNIQUE] INDEX` from 3 SQL files via `re /CREATE\s+(?:UNIQUE\s+)?INDEX\s+(?:IF NOT EXISTS\s+)?(\w+)/gi`; UNIQUE-implied autoindexes from G-32.1 decls; alias rows from `sql/00-overview.md` §Index-name aliases via row regex `\|\s*\`(Idx[A-Z]\w+)\`[^|]*\|\s*\`(Idx[A-Z]\w+)\``, registers both LHS+RHS), `normaliseAutoindex()` (strips `_<digit>` / `_*` suffixes so doc claims `sqlite_autoindex_User_*` match the bare `sqlite_autoindex_User` family key), `findFabricatedIndexes()` + `printReverseReport()`. New `REVERSE_EXEMPT` allow-list with 8 entries grouped into 3 categories: 2 logical-tag aliases (`IdxUser_Email`, `IdxWorkspace_AppDbPath`), 3 prose-rejected names from §"Indexes intentionally NOT created" (`IdxItem_Content`, `IdxItem_CreatedAt`, `IdxComment_AuthorUserId`), 3 v2-deprecated names retained for traceability (`IdxItem_*MirrorOf*`, `IdxMirror_*`). Final exit code = OR of forward+reverse failures. Bumped SSOT `25-g32-...md` v1.0.0→v2.0.0 (added Sub-checks table, removed F-future-G32a from future-ladder, added v2.0.0 change-history row); bumped `02-ci-quality-gates.md` G-32 row description (now describes both directions, both allow-lists, all 4 drift classes caught). Negative-tested by appending `\| \`IdxFabricated_Foo\` \| test \| nope \|` to `06-indexes.md` → runner correctly exits 1 with per-violation report; restored cleanly via cp backup. Master suite confirms ✅ G-32: 13/13 forward + 0/37 fabricated reverse. The 6 pre-existing master failures remain unrelated. Open question (section-aware skips vs flat allow-list — would eliminate 5/8 entries but couples runner to heading strings) deferred to v2.1.0 if allow-list grows past ~15. | 1 (`27-g32-reverse-allowlist-categories.md`) |
| 30 | 2026-04-27 | F-future-G32b — promoted G-32 to v3.0.0 by adding **G-32.3 forward CREATE INDEX coverage** (UNIQUE OR plain). Runner now ~470 lines: added `collectAllCreateIndexNames()` (29 statements across 3 SQL files), `collectIndexAliasMap()` (DDL→prose Map from `sql/00-overview.md` §Index-name aliases — 3 entries: `IdxMirrorMember_ItemId↔IdxMirrorPeerGroupMember_ItemId`, `IdxMirrorMember_MirrorGroupId↔IdxMirrorPeerGroupMember_GroupId`, `IdxMirrorGroup_CanonicalItemId↔IdxMirrorPeerGroup_CanonicalItemId`), `findUndocumentedCreateIndexes()` matches by backticked DDL name OR alias name in `06-indexes.md`, `printCreateIndexReport()`. New `NONUNIQUE_EXEMPT` allow-list (empty). Final exit = OR(G-32.1, G-32.2, G-32.3). **Caught real drift on first run**: `IdxMirrorGroup_CanonicalItemId` (DDL `03-app-indexes.sql:42`) → prose alias `IdxMirrorPeerGroup_CanonicalItemId` was undocumented — only mentioned inline in v1.3.0 deprecation note prose; added new Required-Indexes row to `06-indexes.md` (v1.4.0→v1.5.0 banner + frontmatter bumped) and gate went green. Negative-tested by sed-renaming `IdxItem_DueDate` → `IdxItem_FabricatedDueDate` in `03-app-indexes.sql` → runner correctly exits 1 with per-violation source/alias report; restored cleanly. Bumped SSOT `25-g32-...md` v2.0.0→v3.0.0 (frontmatter + banner; added G-32.3 row to Sub-checks table; removed F-future-G32b from future-ladder; added partial/expression-index scope note explaining name-only matching; logged F-future-G32d for predicate parity if ever needed; appended v3.0.0 change-history row); bumped `02-ci-quality-gates.md` G-32 row description (now "Three-direction check" enumerating all 3 allow-lists + flagging predicate/column drift as out of scope). Header docblock had a `*/` JSDoc-terminator bug from rendering "Idx*/sqlite_autoindex_*" — fixed by rewording to "Idx{Name} / sqlite_autoindex_{T}". Master suite confirms ✅ G-32: 13/13 UNIQUE forward + 0/37 reverse fabricated + 29/29 CREATE INDEX forward. Pre-existing 6 master failures unchanged. Open question (column/predicate parity beyond name matching — needs SQL parser, would invalidate the prose-cell semantics) deferred as F-future-G32d. | 1 (`28-g32-3-name-only-vs-predicate-parity.md`) |
---

## Notes

- Increment after each completed user task.
- One row per task, even if the task logged zero ambiguities.
- When count reaches **40**, set Status to 🔴 EXPIRED and notify the user to review the ambiguity folder.
