# ADRs — Acceptance Criteria

> **Version:** 1.9.0
> **Created:** 2026-04-29 — closes G-08 acceptance-coverage gap (P0 quick-win). **Updated:** 2026-04-29 — v1.1.0..v1.4.0 added AT-ADR-G04..G07; v1.5.0 G08 (ADR-0025); v1.6.0 G09 (ADR-0015); v1.7.0 G10 (ADR-0016); v1.8.0 G11 (ADR-0017); v1.9.0 G12 (ADR-0020); v1.10.0 G13 (ADR-0021); v1.11.0 G14 (ADR-0024) — F-AUDIT-21 CLOSED 7/7; v1.12.0 appended F-AUDIT-15 closure note (task #2); **v1.13.0** added AT-NS-G15 (4 rows AT-NS-SCOPING-*) ratifying `spec/00-scoping.md` corpus boundary SSOT — closes **F-AUDIT-28** (v6 audit MED, +3).
> **Status:** ✅ SSOT — testable acceptance criteria for the ADR governance scope.

> _Fixture: N/A — pure narrative reference, not a testable criterion._


---

## Overview

This file consolidates the testable acceptance criteria that govern the ADR
authoring + lifecycle workflow. Each row is enforced by a CI gate registered
in [`spec/_GATE-REGISTRY.md`](../_GATE-REGISTRY.md); rows without a gate are
DOC-tier and verified by reviewer checklist only.

The criteria are grouped into 3 categories: **shape** (file structure),
**lifecycle** (status / supersede transitions), and **governance** (cross-link
+ AI-Contract integrity).

---

## AT-ADR-G01: ADR file shape

| # | Criterion | Source | Gate |
|---|-----------|--------|------|
| AT-ADR-001 | Every ADR file lives under `spec/00-adrs/NNNN-kebab-case-title.md` (4-digit zero-padded prefix). | [`00-overview.md`](./00-overview.md) §Required sections | `G-00-ADR-NUMBERING` |
| AT-ADR-002 | Every ADR file contains the 8 required sections in canonical order: `## Status`, `## Context`, `## Decision`, `## Consequences`, `## Alternatives Considered`, `## Compliance / Enforcement`, `## Related ADRs`, `## References`. | [`00-overview.md`](./00-overview.md) §Required sections | `G-00-ADR-SHAPE` |
| AT-ADR-003 | The `## Status` line uses one of the closed values: `Accepted`, `Proposed`, `Superseded by ADR-NNNN`, `Deprecated`. | [`00-overview.md`](./00-overview.md) §Lifecycle | `G-00-ADR-STATUS` |

---

## AT-ADR-G02: ADR lifecycle

| # | Criterion | Source | Gate |
|---|-----------|--------|------|
| AT-ADR-004 | When ADR-N supersedes ADR-M, ADR-M's `## Status` MUST flip to `Superseded by ADR-N` in the **same** change. | [`00-overview.md`](./00-overview.md) §Definition of Done | `G-00-ADR-SUPERSEDE` |
| AT-ADR-005 | New ADRs MUST update both index tables in lock-step: `spec/00-adrs/00-overview.md` §Index **and** `spec/00-overview.md` rollup. | [`00-overview.md`](./00-overview.md) §Definition of Done | `G-13-ADR-INDEX-CASCADE` |
| AT-ADR-006 | ADR files are append-only — accepted ADRs are never edited in place except for `## Status` flips and `## Related ADRs` back-links. | [`00-overview.md`](./00-overview.md) §Purpose | DOC (reviewer) |

---

## AT-ADR-G03: Governance + AI-Contract integrity

| # | Criterion | Source | Gate |
|---|-----------|--------|------|
| AT-ADR-007 | Every load-bearing rule cited by a `G-*` gate MUST trace to an ADR (drained via the `G-NS-ADR-MUST-HAS-AT` allow-list). | [`spec/_GATE-REGISTRY.md`](../_GATE-REGISTRY.md) | `G-NS-ADR-MUST-HAS-AT` |
| AT-ADR-008 | The ADR-overview AI Contract block MUST contain the 5 required subsections (`Purpose`, `Audience`, `Expected AI Output`, `Out of Scope`, `Definition of Done`) plus a `### Scoring` table. | [`00-overview.md`](./00-overview.md) §AI Contract | `G-00-OVERVIEW-AI-CONTRACT-COMPLETE` |
| AT-ADR-009 | Every ADR with non-trivial downstream impact MUST list at least one `## Consequences` bullet that cross-links to the spec scope it locks (e.g. `spec/04-database-conventions/`). | [`00-overview.md`](./00-overview.md) §Required sections | `G-00-ADR-CONSEQUENCES-XLINK` (CI, WARN-only initial mode; baseline allow-list at [`_LEDGER-G-00-ADR-CONSEQUENCES-XLINK-BASELINE.md`](./_LEDGER-G-00-ADR-CONSEQUENCES-XLINK-BASELINE.md), 90-day TTL) |



---

## AT-ADR-G04: ADR-0029 — per-(gate, path) ledger shared library

> Ratifies the 5 acceptance tests cited inline by [`0029-per-gate-path-ledger-shared-lib.md`](./0029-per-gate-path-ledger-shared-lib.md) §6. All five are enforced by CI gate `G-13-LEDGER-USES-SHARED-LIB` ([`scripts/spec-hygiene/48-check-ledger-uses-shared-lib.mjs`](../../scripts/spec-hygiene/48-check-ledger-uses-shared-lib.mjs)) except `AT-29-D3-SCHEMA-FROZEN`, which is owned by `G-13-LEDGER-PER-GATE-PATH`.

| # | Criterion | Source | Gate |
|---|-----------|--------|------|
| AT-29-D1-IMPORT-PRESENT | Every runner under `scripts/spec-hygiene/` (excluding `_lib/`) that references `_LEDGER-G-*-EXEMPTIONS.md` MUST also import from `scripts/spec-hygiene/_lib/per-gate-path-ledger.mjs`. | [`0029-per-gate-path-ledger-shared-lib.md`](./0029-per-gate-path-ledger-shared-lib.md) §D1 | `G-13-LEDGER-USES-SHARED-LIB` |
| AT-29-D3-SCHEMA-FROZEN | Every `_LEDGER-G-*-EXEMPTIONS.md` file MUST contain exactly one `## Entries` H2 and a 5-column table with header row `gate \| pathGlob \| entry \| rationale \| addedOn` (case-sensitive, in order). | [`0029-per-gate-path-ledger-shared-lib.md`](./0029-per-gate-path-ledger-shared-lib.md) §D3 | `G-13-LEDGER-PER-GATE-PATH` |
| AT-29-D4-NO-INLINE-GLOBTOREGEXP | No file under `scripts/spec-hygiene/` outside `_lib/` may define `function globToRegExp` or `const globToRegExp =`. | [`0029-per-gate-path-ledger-shared-lib.md`](./0029-per-gate-path-ledger-shared-lib.md) §D4.1 | `G-13-LEDGER-USES-SHARED-LIB` |
| AT-29-D4-NO-INLINE-WALKLEDGER | No file under `scripts/spec-hygiene/` outside `_lib/` may define `function walkLedger`, `function* walkLedger`, or `const walkLedger =`. | [`0029-per-gate-path-ledger-shared-lib.md`](./0029-per-gate-path-ledger-shared-lib.md) §D4.2 | `G-13-LEDGER-USES-SHARED-LIB` |
| AT-29-D4-NO-DIRECT-LEDGER-READ | No file under `scripts/spec-hygiene/` outside `_lib/` may pass a path matching `_LEDGER-G-*-EXEMPTIONS.md` to `fs.readFile` / `fs.readFileSync` directly — ledger I/O MUST flow through `walkLedger()`. | [`0029-per-gate-path-ledger-shared-lib.md`](./0029-per-gate-path-ledger-shared-lib.md) §D4.3 | `G-13-LEDGER-USES-SHARED-LIB` |

---

## AT-ADR-G05: ADR-0030 — audit exemption manifest

> Ratifies the 8 acceptance tests cited inline by [`0030-audit-exemption-manifest.md`](./0030-audit-exemption-manifest.md) §6. All eight are enforced by CI gate `G-00-AUDIT-EXEMPTION-REVIEW` ([`scripts/spec-hygiene/57-check-audit-exemption-review.mjs`](../../scripts/spec-hygiene/57-check-audit-exemption-review.mjs)). Closes **F-AUDIT-31** (LOW — surfaced by re-audit v2; artifact: `/mnt/documents/spec-ai-implementability-audit-v2.json`).

| # | Criterion | Source | Gate |
|---|-----------|--------|------|
| AT-30-I1-MANIFEST-EXISTS | `spec/_AUDIT-EXEMPTIONS.md` MUST exist at the canonical path. | [`0030-audit-exemption-manifest.md`](./0030-audit-exemption-manifest.md) §D1 | `G-00-AUDIT-EXEMPTION-REVIEW` |
| AT-30-I2-SINGLE-H2 | The manifest MUST contain exactly one `## Exemption rows` H2 (case-sensitive). | [`0030-audit-exemption-manifest.md`](./0030-audit-exemption-manifest.md) §D2 | `G-00-AUDIT-EXEMPTION-REVIEW` |
| AT-30-I3-HEADER-SHAPE | The table header row MUST be `pathGlob \| category \| rationale \| closes \| addedOn` (case-sensitive, in order). | [`0030-audit-exemption-manifest.md`](./0030-audit-exemption-manifest.md) §D2 | `G-00-AUDIT-EXEMPTION-REVIEW` |
| AT-30-I4-SPEC-ROOTED | Every row's `pathGlob` MUST start with `spec/`. | [`0030-audit-exemption-manifest.md`](./0030-audit-exemption-manifest.md) §D3 | `G-00-AUDIT-EXEMPTION-REVIEW` |
| AT-30-I5-NO-BLANK-CHEQUE | Every row's `pathGlob` MUST NOT be a corpus-wide pattern (`spec/**`, `spec/**/*`, `spec/**/*.md`, `spec/*`, `**/*`). | [`0030-audit-exemption-manifest.md`](./0030-audit-exemption-manifest.md) §D3 | `G-00-AUDIT-EXEMPTION-REVIEW` |
| AT-30-I6-CITED | Every row's `closes` cell MUST cite at least one of: `AUD-*`, `F-AUDIT-NN`, `F-AUDxx-NN`, `ADR-NNNN`, or the literal `n/a`. | [`0030-audit-exemption-manifest.md`](./0030-audit-exemption-manifest.md) §D3 | `G-00-AUDIT-EXEMPTION-REVIEW` |
| AT-30-I7-ISO-DATE | Every row's `addedOn` MUST be ISO `YYYY-MM-DD`. | [`0030-audit-exemption-manifest.md`](./0030-audit-exemption-manifest.md) §D3 | `G-00-AUDIT-EXEMPTION-REVIEW` |
| AT-30-I8-VISIBILITY | Every CI run MUST print `matched/total (pct%)` so reviewers can detect drift in PR output. | [`0030-audit-exemption-manifest.md`](./0030-audit-exemption-manifest.md) §D3 | `G-00-AUDIT-EXEMPTION-REVIEW` |

---

## AT-ADR-G06: ADR-0031 — warn-only-with-STRICT-flip gate-graduation pattern

> Ratifies the 8 acceptance tests cited inline by [`0031-warn-only-strict-flip-pattern.md`](./0031-warn-only-strict-flip-pattern.md) §6. Six are CI-enforced (gates `G-00-GRADUATION-LEDGER-FRESH`, `G-00-GRADUATION-LEDGER-DATE-DRIFT`, `G-38-AMBIGUOUS-WORDING`); two are DOC-tier (reviewer-enforced, future CI candidates). Closes **F-SPEC-13** (MED — surfaced by spec-vs-impl audit 2026-04-29) and **F-AUDIT-26** (MED — surfaced by re-audit v4; artifact: `/mnt/documents/spec-ai-implementability-audit-v4.json`).

| # | Criterion | Source | Gate |
|---|-----------|--------|------|
| AT-31-D1-CLOSED-MODES | Every hygiene runner under `scripts/spec-hygiene/[0-9][0-9]-*.mjs` whose top-level docstring declares a `STRICT` constant MUST set it to literal `true` or `false` — no expressions, no `process.env` reads. | [`0031-warn-only-strict-flip-pattern.md`](./0031-warn-only-strict-flip-pattern.md) §D1 | DOC (reviewer; future CI candidate — promote when 4th non-literal STRICT appears) |
| AT-31-D2-LEDGER-COVERAGE | Every `_GATE-REGISTRY.md` row whose description contains the literal `**WARN-only**` MUST appear as a row in `_GATE-GRADUATION-LEDGER.md` §Entries (or §Graduated entries). Conversely, every ledger row MUST cite a registry-known gate. | [`0031-warn-only-strict-flip-pattern.md`](./0031-warn-only-strict-flip-pattern.md) §D2 | `G-00-GRADUATION-LEDGER-FRESH` |
| AT-31-D3-MEASURABLE-PREDICATE | Every §Entries row's `flipCriterion` cell MUST contain at least one of the 5 grammar markers: `count =`, `≤`, `consecutive CI`, `targetDate <`, OR a compound `AND`/`OR` joining two such clauses. Forbidden vague tokens (`eventually`, `to-be-determined`, the three-letter unspecified-marker, `next pass`, `event-driven`, `someday`) MUST NOT appear. | [`0031-warn-only-strict-flip-pattern.md`](./0031-warn-only-strict-flip-pattern.md) §D3 | `G-38-AMBIGUOUS-WORDING` (vague-token rejection) + DOC (grammar shape; future CI as `G-00-GRADUATION-LEDGER-CRITERION-SHAPE` once 4th predicate variant emerges) |
| AT-31-D4-FLIP-MECHANISM-CITES-RUNNER | Every §Entries row's `flipMechanism` cell MUST cite a runner path matching `scripts/spec-hygiene/[0-9][0-9]-*.mjs` OR a ledger filename matching `_LEDGER-G-*-EXEMPTIONS.md` (for ledger-driven gates per ADR-0029). | [`0031-warn-only-strict-flip-pattern.md`](./0031-warn-only-strict-flip-pattern.md) §D4 | DOC (reviewer; future CI candidate — promote when 4th non-runner mechanism appears) |
| AT-31-D5-ISO-DATE | Every §Entries row's `targetDate` MUST match `YYYY-MM-DD` AND MUST NOT be `< addedOn`. | [`0031-warn-only-strict-flip-pattern.md`](./0031-warn-only-strict-flip-pattern.md) §D5 | `G-00-GRADUATION-LEDGER-DATE-DRIFT` |
| AT-31-D5-OVERDUE-FAIL | When `targetDate < today` for any §Entries row, gate #61 MUST exit 1 with stderr matching `/G-00-GRADUATION-LEDGER-DATE-DRIFT.*overdue/`. Meta-test at [`scripts/spec-hygiene/_tests/61.test.mjs`](../../scripts/spec-hygiene/_tests/61.test.mjs) locks the visibility-line contract. | [`0031-warn-only-strict-flip-pattern.md`](./0031-warn-only-strict-flip-pattern.md) §D5 | `G-00-GRADUATION-LEDGER-DATE-DRIFT` |
| AT-31-D6-PROTOCOL-COMPLETE | A graduation PR MUST modify all 3 of: (a) the runner per `flipMechanism`, (b) `_GATE-GRADUATION-LEDGER.md` (row moved to §Graduated entries with `graduatedOn` cell appended), (c) `_GATE-REGISTRY.md` (WARN-only parenthetical removed). PRs missing any leg MUST be rejected. | [`0031-warn-only-strict-flip-pattern.md`](./0031-warn-only-strict-flip-pattern.md) §D6 | DOC (reviewer; future CI as task #42 — `Graduated entries` L10 validation in gate #60) |
| AT-31-D7-NO-RETROACTIVE-DATES | A new §Entries row whose `targetDate < addedOn` MUST be rejected at PR time. Date-sanity branch of gate #61 enforces. | [`0031-warn-only-strict-flip-pattern.md`](./0031-warn-only-strict-flip-pattern.md) §D7 | `G-00-GRADUATION-LEDGER-DATE-DRIFT` |
| AT-31-PROTOCOL-COOLING-WINDOW | The 6-step flip protocol §D6.1 MUST verify `flipCriterion` true for **7 consecutive CI runs** before steps 2–6. Skipping this step is forbidden. | [`0031-warn-only-strict-flip-pattern.md`](./0031-warn-only-strict-flip-pattern.md) §D6 | DOC (reviewer; CI tracking via ledger note column — future) |

---

## AT-ADR-G07: ADR-0023 — loader↔queue contract

> Ratifies the 8 acceptance tests cited inline by [`0023-route-loaders-offline-queue-interaction.md`](./0023-route-loaders-offline-queue-interaction.md) §"Gates Touched". Closes **F-AUDIT-27** (HIGH — recalibrated audit v5: avoidance of high-impact ADR-AT pair authoring; first ratification of the load-bearing loader↔queue ADR per memory `mem://architecture/adr-invariants-15-25`). Each row below maps 1:1 to a `G-23-*` gate; rows marked `DOC` are reviewer-checklist until the CI runner is authored (tracked separately in the gate registry).

| # | Criterion | Source | Gate |
|---|-----------|--------|------|
| AT-23-L1-LOADER-MIRROR-FIRST | Every React Router v7 `loader` MUST resolve from the IndexedDB local mirror before any network I/O. Loader bodies MUST NOT contain `fetch(`, `axios.`, or any `import` from `src/lib/api/*` egress modules — only mirror reads via `src/lib/mirror/loaders/*`. Violation = loader breaks the 16 ms p95 budget AND renders blank screens when offline. | [`0023-route-loaders-offline-queue-interaction.md`](./0023-route-loaders-offline-queue-interaction.md) §Decision D1 + Gates Touched §1 | `G-23-LOADER-MIRROR-FIRST` |
| AT-23-A1-ACTION-ENQUEUE-ONLY | Every React Router v7 `action` MUST commit a single IDB transaction that writes BOTH the optimistic mirror update AND the queue entry. Direct server calls (`fetch`/`axios`) inside action bodies are forbidden — the queue worker is the sole egress per ADR-0010 FIFO contract. | [`0023-route-loaders-offline-queue-interaction.md`](./0023-route-loaders-offline-queue-interaction.md) §Decision D2 + Gates Touched §2 | `G-23-ACTION-ENQUEUE-ONLY` |
| AT-23-S1-COLD-OFFLINE-SHELL | The root loader MUST return the `OfflineColdStartShell` sentinel (typed `{ Status: "OfflineColdStart"; Attributes: {}; Results: [] }` per ADR-0019 envelope) when (a) the IDB mirror is empty AND (b) `navigator.onLine === false`. Throwing to an error boundary in this state is forbidden — the cold-offline shell renders a neutral landing UI, not an error page. | [`0023-route-loaders-offline-queue-interaction.md`](./0023-route-loaders-offline-queue-interaction.md) §Decision D3 + Gates Touched §3 | `G-23-COLD-OFFLINE-SHELL` |
| AT-23-D4-WARM-LOADER-16MS | Warm-start loader p95 latency (mirror hit, no revalidation network call) MUST be ≤ 16 ms (one frame at 60 Hz) measured by the loader-perf harness over a 1000-loader sample. The 16 ms ceiling is non-negotiable: a slower loader stalls navigation and triggers the visible-spinner threshold. | [`0023-route-loaders-offline-queue-interaction.md`](./0023-route-loaders-offline-queue-interaction.md) §Decision D4 + Gates Touched §4 | `G-23-WARM-LOADER-16MS` (DOC; perf harness pending) |
| AT-23-R1-RECONNECT-LOCK | The queue worker MUST hold an exclusive Web Lock (`navigator.locks.request('queue-drain', { mode: 'exclusive' }, …)`) for the entire reconnect drain. Concurrent loader revalidation MUST acquire the same lock in `'shared'` mode and yield until the drain completes. This guarantees create-then-rename ordering survives reconnect. | [`0023-route-loaders-offline-queue-interaction.md`](./0023-route-loaders-offline-queue-interaction.md) §Decision D5 + Gates Touched §5 | `G-23-RECONNECT-LOCK` |
| AT-23-L2-LOADER-NO-MUTATE | Loaders MUST NOT call any queue-public API (`enqueue`, `flush`, `cancel`) and MUST NOT write to the mirror outside the React Router revalidation cache. Read-only purity is required so revalidation can be canceled mid-flight without leaving partial state. | [`0023-route-loaders-offline-queue-interaction.md`](./0023-route-loaders-offline-queue-interaction.md) §Decision D6 + Gates Touched §6 | `G-23-LOADER-NO-MUTATE` |
| AT-23-A2-ACTION-NO-THROW | Actions MUST resolve with a typed Status envelope (`{ Status: "Ok" \| "ValidationFailed" \| "QueueFull" \| "Unauthorized"; Attributes; Results; Errors? }`) and MUST NOT throw to the error boundary. The boundary catches loader errors only — action failures are routed to inline form UI per ADR-0017 boundary partitioning. | [`0023-route-loaders-offline-queue-interaction.md`](./0023-route-loaders-offline-queue-interaction.md) §Decision D7 + Gates Touched §7 | `G-23-ACTION-NO-THROW` |
| AT-23-F1-FETCHER-SAME-PATH | `useFetcher().submit()` calls MUST route through the identical mirror+queue IDB transaction used by route actions. A separate egress path for fetcher submissions is forbidden — divergent paths break FIFO ordering when a fetcher submit interleaves with a navigation-action submit on the same item. | [`0023-route-loaders-offline-queue-interaction.md`](./0023-route-loaders-offline-queue-interaction.md) §Decision D8 + Gates Touched §8 | `G-23-FETCHER-SAME-PATH` |

---




- Gates `G-00-ADR-SHAPE`, `G-00-ADR-NUMBERING`, `G-00-ADR-STATUS`, `G-00-ADR-SUPERSEDE`, and `G-13-ADR-INDEX-CASCADE` are already implemented in `scripts/spec-hygiene/`.
- `G-00-ADR-CONSEQUENCES-XLINK` was authored 2026-04-29 in [`scripts/spec-hygiene/52-check-adr-consequences-xlink.mjs`](../../scripts/spec-hygiene/52-check-adr-consequences-xlink.mjs) (WARN-only; baseline allow-list at [`_LEDGER-G-00-ADR-CONSEQUENCES-XLINK-BASELINE.md`](./_LEDGER-G-00-ADR-CONSEQUENCES-XLINK-BASELINE.md), 90-day TTL).
- This file deliberately defers per-ADR criteria (e.g. "ADR-0026 §D6 mandates `OwnerId` egress") to the consuming spec scope's `97-acceptance-criteria.md` — the present file covers only the *governance* of ADRs, not their content.

---

## Related

- [`97a-acceptance-criteria-fixtures.md`](./97a-acceptance-criteria-fixtures.md) — Sibling I/O fixtures for AT-29-* and AT-30-* (closes AT-FIX-01)
- [`spec/00-adrs/00-overview.md`](./00-overview.md) — ADR authoring contract + index
- [`spec/_GATE-REGISTRY.md`](../_GATE-REGISTRY.md) — gate ↔ AT bindings
- [`spec/00-overview.md`](../00-overview.md) — top-level ADR rollup table

---

## AT-ADR-G09: ADR-0015 — Twelve `ItemType` enum (closed, lowercase, mirror-excluded)

> Ratifies the 6 decision clauses (D1..D6) of [`0015-twelve-itemtypes-enum.md`](./0015-twelve-itemtypes-enum.md). 7 rows: D1 split into D1-LIST + D1-ORDER. Two gates already CI-active (`G-15-ITEMTYPE-LOWERCASE`, `G-20-ITEMTYPE-CLOSED-12`); remaining are DOC-tier with named future-CI gates per ADR-0031. Closes 2/7 of **F-AUDIT-21**; remaining: ADR-0016, 0017, 0020, 0021, 0024.

| # | Criterion | Source | Gate |
|---|-----------|--------|------|
| AT-15-D1-LIST | The `ItemType` enum MUST contain exactly the twelve values `{bullet, h1, h2, h3, paragraph, todo, numbered, board, dashboard, quote, code, divider}` — no more, no fewer. Notably, `mirror` MUST NOT appear (D3) and `dashboard` MUST appear (post-AUDIT-03 correction). Pre-AUDIT-03 sets that include `mirror` are gate violations. | [`0015-twelve-itemtypes-enum.md`](./0015-twelve-itemtypes-enum.md) §D1 + §D3 | `G-20-ITEMTYPE-CLOSED-12` |
| AT-15-D1-ORDER | The twelve values MUST appear in the canonical order `bullet → h1 → h2 → h3 → paragraph → todo → numbered → board → dashboard → quote → code → divider` in all three SSOTs (`spec/20-enums-index.md` §3.5, `spec/32-ui-design/02-state-and-data/03-data-types.md`, `src/types/index.ts`). Alphabetical, render-grouped, or DB-insertion order is forbidden — diff review depends on the canonical order. | [`0015-twelve-itemtypes-enum.md`](./0015-twelve-itemtypes-enum.md) §D1 + §D6 | DOC (reviewer; future CI as `G-15-ITEMTYPE-CANONICAL-ORDER` — diff three SSOTs) |
| AT-15-D2-LOWERCASE | All `ItemType` values MUST be lowercase strings in TS source (`"bullet"`, never `"Bullet"`), PHP backed-enum values, Go string labels, SQLite `CHECK(ItemType IN (...))` literals, and JSON wire payloads. PascalCase (`"Bullet"`), SCREAMING_SNAKE (`"BULLET"`), and kebab-case (`"item-bullet"`) are all forbidden. This is a deliberate exception to Universal Rule #1, granted by Rule #10 + ADR-0015 only. | [`0015-twelve-itemtypes-enum.md`](./0015-twelve-itemtypes-enum.md) §D2 + Universal Rule #10 | `G-15-ITEMTYPE-LOWERCASE` |
| AT-15-D3-NO-MIRROR-ITEMTYPE | The string `"mirror"` MUST NOT appear as an `ItemType` value in any of the three SSOTs, in any DB CHECK constraint, in any wire-format fixture, or in any TS exhaustiveness switch. Mirroring is a peer-group relation per ADR-0005 — materialised as rows in the `Mirror` table. "Turn into mirror" is therefore not a legal `ItemType` mutation; it MUST go through the peer-group join path. | [`0015-twelve-itemtypes-enum.md`](./0015-twelve-itemtypes-enum.md) §D3 + ADR-0005 | `G-20-NO-MIRROR-ITEMTYPE` |
| AT-15-D4-CHILD-RENDERING-DUALITY | The two values `board` and `dashboard` MUST control how the item's *direct children* are rendered (Kanban columns and depth-1 card grid respectively). The other ten values MUST render the item itself. Render-switch implementations MUST treat `board`/`dashboard` as branch-on-children, not branch-on-self. Collapsing the two semantics into a single render path is forbidden — it breaks the `mem://features/board-view` and `mem://features/dashboard-view` contracts. | [`0015-twelve-itemtypes-enum.md`](./0015-twelve-itemtypes-enum.md) §D4 + `mem://features/board-view` + `mem://features/dashboard-view` | DOC (reviewer; future CI as `G-15-CHILD-RENDERING-DUALITY` — AST scan of render switch) |
| AT-15-D5-CLOSED-SET | Adding, removing, renaming, or re-casing any `ItemType` value MUST require a superseding ADR. Runtime extension via plugin registry, env-var-driven enum, or string fallback for unknown values is forbidden. Parsing an unknown `ItemType` value MUST be a hard parse error per `spec/20-enums-index.md` §1 rule 6 — silent default fallback is forbidden. | [`0015-twelve-itemtypes-enum.md`](./0015-twelve-itemtypes-enum.md) §D5 + `spec/20-enums-index.md` §1 rule 6 | `G-20-ITEMTYPE-CLOSED-12` (additions/removals) + DOC (reviewer; runtime-extension scan) |
| AT-15-D6-TRI-SSOT-LOCKSTEP | The three SSOTs (`spec/20-enums-index.md` §3.5 enum table, `spec/32-ui-design/02-state-and-data/03-data-types.md` UI taxonomy, `src/types/index.ts` `ItemType` literal union) MUST list exactly the same 12 values in the same canonical order (per AT-15-D1-ORDER). CI MUST fail any PR that desyncs the three. Note: `mem://features/core-mechanics` is currently out of lockstep (lists `mirror` instead of `dashboard`) and is tracked as a known follow-up — does not waive this gate for code SSOTs. | [`0015-twelve-itemtypes-enum.md`](./0015-twelve-itemtypes-enum.md) §D6 | DOC (reviewer; future CI as `G-15-ITEMTYPE-TRI-SSOT` — three-way diff) |

---

## AT-ADR-G10: ADR-0016 — Fractional-index `SortOrder` (lexicographic string, base-62)

> Ratifies the 7 decision clauses (D1..D7) of [`0016-fractional-index-sortorder.md`](./0016-fractional-index-sortorder.md). All 7 rows DOC-tier with named future-CI gates per ADR-0031 — `G-16-*` series promotes when fractional-indexing lib lands. Closes 3/7 of **F-AUDIT-21**; remaining: ADR-0017, 0020, 0021, 0024.

| # | Criterion | Source | Gate |
|---|-----------|--------|------|
| AT-16-D1-STRING-SHAPE | `Item.SortOrder` MUST be `TEXT NOT NULL` in SQLite, `string` in TypeScript (`Item.sortOrder: string`), and `string` on the wire (`"SortOrder": "a0V"`). Numeric representations (`number`, `int`, `float`, `decimal`) are forbidden in all three layers. The stale `number (fractional)` type-def at `spec/31-app/01-features/01-information-model.md:92` and any `int SortOrder` Go example are tracked corrections — code SSOT (`src/types/index.ts`) is the load-bearing layer. | [`0016-fractional-index-sortorder.md`](./0016-fractional-index-sortorder.md) §D1 | DOC (reviewer; future CI as `G-16-SORTORDER-STRING-SHAPE` — AST scan asserts `sortOrder: string` in `src/types/index.ts` + `TEXT NOT NULL` in DDL fixtures) |
| AT-16-D2-BASE62-ALPHABET | The fractional-index alphabet MUST be exactly the 62 ASCII symbols `[0-9A-Za-z]` in lexicographic byte order (`0`<`9`<`A`<`Z`<`a`<`z`). Comparison MUST use plain byte-wise lexicographic ordering — SQLite default `BINARY` collation, JS `<` operator. Locale-aware collation (`NOCASE`, `Intl.Collator`), Unicode characters above U+007E, and any non-base-62 symbol in a generated key are forbidden. | [`0016-fractional-index-sortorder.md`](./0016-fractional-index-sortorder.md) §D2 | DOC (reviewer; future CI as `G-16-BASE62-ALPHABET` — runtime fuzz on the generator asserts every output char ∈ alphabet) |
| AT-16-D3-GENERATION-RULES | The generator MUST follow these four rules: (a) first child of empty parent → `"a0"`; (b) append after sibling `S` → `S + "1"`; (c) prepend before sibling `F` → key strictly `<` `F` lexicographically (decrement-or-shift); (d) insert between `A < B` → key `K` such that `A < K < B` via digit-walk-then-midpoint, suffix-append on adjacent digits. Hand-rolled `(a + b) / 2` numeric midpoint is forbidden — implementations MUST use a vetted fractional-indexing library (`fractional-indexing`, `mudder.js`, or equivalent). | [`0016-fractional-index-sortorder.md`](./0016-fractional-index-sortorder.md) §D3 | DOC (reviewer; future CI as `G-16-GENERATION-RULES` — property-based test asserts `A < gen(A,B) < B` over 10⁴ random pairs) |
| AT-16-D4-SINGLE-ROW-WRITE | Inserting an item between two siblings MUST mutate **only** the new item's `SortOrder` row. Sibling rows MUST remain byte-identical (verified by `AT-APP-04` side-effects clause). Bulk reorder of N items MUST produce exactly N single-row `SortOrder` writes — full-list renumbering, parent-children sweep, or any O(N) sibling-touching write pattern is forbidden outside of D5 rebalance. | [`0016-fractional-index-sortorder.md`](./0016-fractional-index-sortorder.md) §D4 + cross-link `AT-APP-04` | DOC (reviewer; future CI as `G-16-SINGLE-ROW-WRITE` — IDB transaction inspector asserts touched-row count = mutated-item count) |
| AT-16-D5-REBALANCE-TRIGGER | Rebalance MUST trigger when EITHER (a) a newly-generated key would exceed **64 bytes** in length, OR (b) consecutive midpoint-suffix appends to the same key prefix exceed **6** within a single insert cluster. Rebalance scope MUST be limited to children of the affected `ParentId` — full-table or root-scope rebalance is forbidden. Rebalance MUST execute as a single transaction and emit exactly one `SortOrderRebalanced` audit event per affected parent (replay-safe under ADR-0010 FIFO). The legacy numeric `< 0.0001` threshold from `mem://features/editor-core` is superseded and meaningless. | [`0016-fractional-index-sortorder.md`](./0016-fractional-index-sortorder.md) §D5 | DOC (reviewer; future CI as `G-16-REBALANCE-TRIGGER` — unit test pins the 64-byte and 6-append thresholds + audit-event emission) |
| AT-16-D6-CONCURRENCY-TIEBREAK | Two clients computing the same insertion slot MUST be tolerated as a non-error: keys MAY collide (identical string) OR interleave (different strings in the same slot). Tiebreak chain on display order MUST be: (1) `SortOrder` lexicographic; (2) on identical `SortOrder`, ADR-0010 field-level LWW on `(ServerTs, UserId)`; (3) on identical LWW pair, lexicographic `Item.Id`. Clients MUST NOT assume `SortOrder` is unique within a parent — UNIQUE constraint on `(ParentId, SortOrder)` is forbidden. | [`0016-fractional-index-sortorder.md`](./0016-fractional-index-sortorder.md) §D6 + ADR-0010 + ADR-0026 | DOC (reviewer; future CI as `G-16-NO-UNIQUE-CONSTRAINT` — DDL grep rejects `UNIQUE.*SortOrder` patterns) |
| AT-16-D7-WIRE-AND-DDL | REST envelope (ADR-0004) MUST serialise as `"SortOrder": "a0V"` — PascalCase key, JSON string value (never quoted number, never raw number). SQLite column MUST be `SortOrder TEXT NOT NULL` (singular DDL per ADR-0001). The required composite index `CREATE INDEX Item_ParentId_SortOrder ON Item(ParentId, SortOrder)` MUST exist — child-listing queries MUST use this index path. Missing index = automatic gate failure (would force table scan on every page render). | [`0016-fractional-index-sortorder.md`](./0016-fractional-index-sortorder.md) §D7 + ADR-0001 + ADR-0004 | DOC (reviewer; future CI as `G-16-WIRE-AND-DDL` — schema fixture asserts column type + index existence; envelope fixture asserts string serialisation) |

---

## AT-ADR-G11: ADR-0017 — Eight UI error boundaries + 1000-item virtualization

> Ratifies the 6 decision clauses (D1..D6) of [`0017-eight-error-boundaries-ui-virtualization.md`](./0017-eight-error-boundaries-ui-virtualization.md). 7 rows: D1 split into D1-COUNT + D1-NAMES, D5 split into D5-LIB + D5-BEHAVIOR. One gate already named (`G-22-ERROR-BOUNDARIES-EXACTLY-8`); remaining DOC-tier with named `G-17-*` future-CI gates per ADR-0031. Closes 4/7 of **F-AUDIT-21**; remaining: ADR-0020, 0021, 0024.

| # | Criterion | Source | Gate |
|---|-----------|--------|------|
| AT-17-D1-COUNT-EXACTLY-8 | The mounted React tree MUST contain exactly **8** error boundaries — not 1, not 7, not 9. AST scan of `src/` MUST find exactly 8 components extending `React.Component` with a `componentDidCatch` method (or 8 uses of `react-error-boundary`'s `ErrorBoundary`). A single root boundary is forbidden; nested boundaries beyond these 8 are forbidden unless added by a superseding ADR. | [`0017-eight-error-boundaries-ui-virtualization.md`](./0017-eight-error-boundaries-ui-virtualization.md) §D1 | `G-22-ERROR-BOUNDARIES-EXACTLY-8` |
| AT-17-D1-NAMES-CLOSED-SET | The 8 boundaries MUST be named exactly: `AppErrorBoundary`, `RouterErrorBoundary`, `NavbarBoundary`, `SidebarBoundary`, `ContentAreaBoundary`, `EditorBoundary`, `PanelBoundary`, `ModalBoundary`. Renaming any of them, splitting one into sub-boundaries, or using anonymous class components is forbidden. AT-DEBUGGINGTYPESCRIPT-10 (currently asserts one top-level boundary) is hereby obsoleted and MUST be rewritten to assert these 8 names. | [`0017-eight-error-boundaries-ui-virtualization.md`](./0017-eight-error-boundaries-ui-virtualization.md) §D1 table | DOC (reviewer; future CI as `G-17-BOUNDARY-NAMES-CLOSED` — AST scan asserts the closed name set) |
| AT-17-D2-INDEPENDENCE | Each boundary's `componentDidCatch` MUST catch errors isolated to its subtree without unmounting siblings. `componentDidCatch` in boundaries #2–#8 MUST NOT call `window.location.reload()`, `window.location.assign()`, `history.replaceState()`, or otherwise unmount peer regions. Only `AppErrorBoundary` (#1) MAY offer a full-page reload action. Throwing within a fallback MUST NOT propagate to a sibling boundary — fallbacks MUST be defensive (no I/O, no derived state). | [`0017-eight-error-boundaries-ui-virtualization.md`](./0017-eight-error-boundaries-ui-virtualization.md) §D2 | DOC (reviewer; future CI as `G-17-BOUNDARY-INDEPENDENCE` — AST grep rejects `window.location.reload` inside non-AppErrorBoundary `componentDidCatch`) |
| AT-17-D3-FALLBACK-CONTRACT | Every fallback MUST satisfy all 5 sub-rules: (a) region-appropriate copy — generic "Something went wrong" is forbidden in boundaries #2–#8; (b) display the error code from `spec/03-error-manage/02-error-architecture` when present in `error.cause`; (c) call the structured logger from `componentDidCatch` (no `console.error` only); (d) expose at least one recovery action (Retry / Close / Navigate-away button); (e) use semantic design tokens only — no hardcoded colors, per ADR-0012. | [`0017-eight-error-boundaries-ui-virtualization.md`](./0017-eight-error-boundaries-ui-virtualization.md) §D3 + ADR-0012 | DOC (reviewer; future CI as `G-17-FALLBACK-CONTRACT` — snapshot tests per fallback assert the 5 sub-rules) |
| AT-17-D4-VIRTUALIZATION-THRESHOLD | Any list, tree level, or grid that *can* exceed **1,000 visible items** in a single scrollable container MUST be virtualized. Concretely: sidebar outline tree (expanded subtree > 1000), search results, trash view, mirror peer-group lists, multi-select clipboards > 1000 entries. The 250-item view cap (ADR-0008) is the *default-view* cap; the 1000-item rule is the *virtualization-required* cap and applies independently to views where the 250 cap does not apply. Rendering > 1000 DOM rows in a non-virtualized container — even briefly during a transition — is forbidden. | [`0017-eight-error-boundaries-ui-virtualization.md`](./0017-eight-error-boundaries-ui-virtualization.md) §D4 + §D6 | DOC (reviewer; future CI as `G-17-VIRTUALIZATION-THRESHOLD` — runtime DOM-row count assertion in E2E for the 5 listed views) |
| AT-17-D5-LIB-PINNED | Virtualization MUST use `@tanstack/react-virtual` as the **sole** permitted virtualizer. Its version MUST be pinned in `package.json` (no `^` or `~` range, same convention as ADR-0011's Axios pin). `react-window`, `react-virtuoso`, hand-rolled `IntersectionObserver` virtualization, and CSS `content-visibility: auto` as a virtualization substitute are all forbidden. | [`0017-eight-error-boundaries-ui-virtualization.md`](./0017-eight-error-boundaries-ui-virtualization.md) §D5 | DOC (reviewer; future CI as `G-17-VIRTUALIZER-PINNED` — `package.json` grep + import-source AST scan) |
| AT-17-D5-BEHAVIOR | Every virtualized container MUST satisfy all 4 behavioural sub-rules: (a) keyboard focus survives row recycling on scroll (no focus loss when a focused row scrolls out and back into the viewport); (b) scroll position MUST remain stable on item insert/delete (no visible jump); (c) drag-and-drop MUST work across the virtualized boundary per `mem://features/editor-core` DnD rules (a drag started inside the viewport MUST survive recycling of the source row); (d) every rendered row MUST expose a stable `data-testid` for E2E reach. | [`0017-eight-error-boundaries-ui-virtualization.md`](./0017-eight-error-boundaries-ui-virtualization.md) §D5 + `mem://features/editor-core` | DOC (reviewer; future CI as `G-17-VIRTUALIZER-BEHAVIOR` — Playwright suite asserts focus, scroll-stability, DnD, and testid presence) |

---

## AT-ADR-G12: ADR-0020 — Branded `ItemId` / `OwnerId` opaque-string types

> Ratifies the 6 decision clauses (D1..D6) of [`0020-branded-itemid-ownerid.md`](./0020-branded-itemid-ownerid.md). 6 rows. Two reference existing patterns in `src/types/index.ts` (15–22, 115–131); D3+D4 extend the helpers from non-empty validation to full regex validation. All DOC-tier with named `G-20-*` future-CI gates per ADR-0031. Closes 5/7 of **F-AUDIT-21**; remaining: ADR-0021, 0024.

| # | Criterion | Source | Gate |
|---|-----------|--------|------|
| AT-20-D1-BRAND-MANDATORY | Every entity ID field in TypeScript MUST be a branded type, never a raw `string`. The two ratified brands MUST be defined in `src/types/index.ts` exactly as: `type ItemId = Brand<string, "ItemId">;` and `type OwnerId = Brand<string, "OwnerId">;` — using the `unique symbol` `__brand` pattern. Field declarations like `parentId: string`, `ownerId: string`, `itemId: string` in TS source or interface declarations are forbidden — they MUST use the branded type. New entity ID types (e.g. `MirrorId`, `ShareId`, `RoleId`) MUST be defined in `src/types/index.ts` (single SSOT) and follow the same `Brand<string, "Foo">` shape. | [`0020-branded-itemid-ownerid.md`](./0020-branded-itemid-ownerid.md) §D1 | DOC (reviewer; future CI as `G-20-BRAND-MANDATORY` — AST scan rejects `: string` on any field whose name matches `/Id$/i` outside `src/types/index.ts`) |
| AT-20-D2-BOUNDARY-CONSTRUCTION | Branded values MUST be constructed exclusively via `as<Brand>()` helper functions (`asItemId`, `asOwnerId`, …) at exactly three trust boundaries: (1) REST envelope deserialisation per ADR-0019; (2) URL/route parameter parsing inside ADR-0018 v7 loaders (never inside components); (3) IndexedDB local-mirror rehydration per ADR-0010 offline queue. The `as Foo` cast syntax (`raw as ItemId`, `<ItemId>raw`) outside the helper function bodies themselves is forbidden — CI MUST flag it. Inline branding in components, hooks, or selectors is forbidden. | [`0020-branded-itemid-ownerid.md`](./0020-branded-itemid-ownerid.md) §D2 + ADR-0018 + ADR-0019 + ADR-0010 | DOC (reviewer; future CI as `G-20-BOUNDARY-CONSTRUCTION` — AST scan rejects `as ItemId` / `as OwnerId` outside `src/types/index.ts` helper bodies) |
| AT-20-D3-RUNTIME-SHAPE | `ItemId` and `OwnerId` runtime values MUST be opaque ASCII strings matching the regex `^[A-Za-z0-9_-]{8,64}$` on the wire and in storage. The exact generator (UUID v4, UUID v7, ULID, `itm_<ulid>` prefix) is implementation choice but MUST satisfy: ASCII-safe (no Unicode, no path-unsafe chars `/`, `\`, `?`, `#`, `&`, `=`, ` `), length 8–64 bytes inclusive, ≥ 2^64 entropy. Lexicographic sortability is preferred but not required. URN forms (`urn:uuid:...`) are forbidden by the regex — migrating requires a superseding ADR. | [`0020-branded-itemid-ownerid.md`](./0020-branded-itemid-ownerid.md) §D3 | DOC (reviewer; future CI as `G-20-RUNTIME-SHAPE` — fixture validator runs the regex against every ID-shaped field in the wire-fixture corpus) |
| AT-20-D4-HELPER-CONTRACT | The `as<Brand>()` helper functions MUST satisfy all four sub-rules: (a) signature accepts `string` (NOT `unknown` — the boundary parser is responsible for the `unknown → string` narrowing first); (b) body validates per D3 (regex match + length 8–64); (c) on validation failure, throws a tagged `InvalidIdError` (NOT a generic `Error`, NOT a returned `null`, NOT silent coercion); (d) the helper is the only export shape for constructing the brand — no class, no factory object, no curried builder. The current implementation (non-empty check only) MUST be extended to the full regex validation as a follow-up. | [`0020-branded-itemid-ownerid.md`](./0020-branded-itemid-ownerid.md) §D4 | DOC (reviewer; future CI as `G-20-HELPER-CONTRACT` — vitest pins helper signature, validation regex, and `InvalidIdError` throw) |
| AT-20-D5-WIRE-PLAIN-STRING | On the wire (REST envelope per ADR-0019) and in SQLite columns, IDs MUST serialise as plain JSON strings — no wrapper object, no type-tag prefix in the JSON shape itself. Branding is a TypeScript-only discipline. The WP/PHP/SQLite layer treats IDs as `string` with a runtime regex check matching the D3 regex (`^[A-Za-z0-9_-]{8,64}$`); PHP MUST NOT attempt to mirror the brand pattern (no PHP enum, no value object) — runtime regex validation is sufficient and equivalent. | [`0020-branded-itemid-ownerid.md`](./0020-branded-itemid-ownerid.md) §D5 + ADR-0019 | DOC (reviewer; future CI as `G-20-WIRE-PLAIN-STRING` — envelope fixture asserts ID fields are JSON strings, not objects) |
| AT-20-D6-SPEC-CONSISTENCY-SWEEP | All spec lines typing entity ID fields as plain `string` MUST be corrected to use the branded type. Tracked targets include: `spec/02-coding-guidelines/00-overview.md` line 96/100 (`parentId: string, ownerId: string` → branded); `spec/01-spec-authoring-guide/13-feature-file-template.md` line 45 (`itemId | string` → `itemId | ItemId`); any other field name ending in `Id`/`id` typed as `string` outside `src/types/index.ts` brand definitions. Folded into the P69 stale-typing sweep — does NOT block this AT row's gate but blocks F-AUDIT-21 closure. | [`0020-branded-itemid-ownerid.md`](./0020-branded-itemid-ownerid.md) §D6 | DOC (reviewer; future CI as `G-20-SPEC-CONSISTENCY-SWEEP` — markdown grep rejects `: string` on `*Id` fields in spec/ examples; integrates with P69) |

---

## AT-ADR-G13: ADR-0021 — Undo cap (100, in-memory, per-tab) + offline queue (unbounded, IndexedDB)

> Ratifies the 6 decision clauses (D1..D6) of [`0021-undo-100-offline-queue-unbounded.md`](./0021-undo-100-offline-queue-unbounded.md). 8 rows: D1 split into D1-CAP + D1-MEMORY-PERTAB + D1-REDO-INVALIDATION; D2 split into D2-UNBOUNDED-IDB + D2-NO-LOCALSTORAGE + D2-QUOTA-HARD-ERROR. Two enforcement-tier rows (D1-CAP, D2-NO-LOCALSTORAGE) named with `G-25-*` gates already specified in the ADR; remaining DOC-tier with named future-CI gates per ADR-0031. Closes 6/7 of **F-AUDIT-21**; remaining: ADR-0024.

| # | Criterion | Source | Gate |
|---|-----------|--------|------|
| AT-21-D1-CAP-100 | The undo stack and the redo stack MUST each cap at exactly **100 entries** (200 total at peak). On the 101st push, the oldest entry MUST be evicted FIFO — silent drop is the contract, not an error. Hard-coded smaller caps (`MAX_UNDO = 50`), unbounded stacks (no cap), and per-document caps (cap-per-route instead of per-tab) are forbidden. Granularity: one entry per atomic user action per `mem://features/editor-core` action-classification rules (text edits debounced at 500ms idle, structural moves, completion toggles, bulk operations each = 1 entry). | [`0021-undo-100-offline-queue-unbounded.md`](./0021-undo-100-offline-queue-unbounded.md) §D1 | `G-25-UNDO-CAP-100` |
| AT-21-D1-MEMORY-PERTAB | Undo and redo stacks MUST live in volatile in-memory state (React/Zustand) only — never persisted to `localStorage`, `sessionStorage`, IndexedDB, or any other durable store. Stacks MUST NOT survive page reload, tab close, or browser restart. Stacks MUST NOT be shared across browser tabs of the same account — `BroadcastChannel`, `SharedWorker`, and `storage` event wiring of undo state are forbidden. Each tab maintains its own independent 100-deep stack pair. | [`0021-undo-100-offline-queue-unbounded.md`](./0021-undo-100-offline-queue-unbounded.md) §D1 + §D4 | `G-25-UNDO-IN-MEMORY-ONLY` + `G-25-UNDO-PER-TAB` |
| AT-21-D1-REDO-INVALIDATION | Any new user action (not an undo or redo itself) MUST clear the redo stack atomically with the new push. Partial clears, deferred clears, or "redo branches" (preserving redo across new actions) are forbidden — standard linear undo semantics only. The redo-clear MUST happen synchronously in the same state transition as the new undo push (no race window where redo is stale). | [`0021-undo-100-offline-queue-unbounded.md`](./0021-undo-100-offline-queue-unbounded.md) §D1 | DOC (reviewer; future CI as `G-21-REDO-INVALIDATION` — vitest asserts `state.redo === []` after every non-undo/redo dispatch) |
| AT-21-D2-UNBOUNDED-IDB | The offline mutation queue MUST have no spec-level capacity cap — bounded only by the user's IndexedDB quota. Constants named `MAX_QUEUE_SIZE`, `QUEUE_CAP`, `QUEUE_LIMIT`, `OFFLINE_BUFFER_SIZE`, or any equivalent ring-buffer eviction logic in the offline queue path are forbidden. Storage substrate MUST be IndexedDB per ADR-0010, with a single ordered store keyed by client-generated monotonic `LocalSeq: number` preserving FIFO. The queue MUST survive tab close, OS crash, and browser process restart. | [`0021-undo-100-offline-queue-unbounded.md`](./0021-undo-100-offline-queue-unbounded.md) §D2 + ADR-0010 | `G-25-QUEUE-UNBOUNDED` |
| AT-21-D2-NO-LOCALSTORAGE | The offline queue module MUST NOT call `localStorage.setItem`, `localStorage.getItem`, `sessionStorage.*`, or any synchronous Web Storage API for queue data. Cross-references ADR-0010 substrate decision. The stale `mem://features/editor-core` line 7 claim ("500-action offline local queue (localStorage)") is superseded by D6 and MUST NOT be implemented. | [`0021-undo-100-offline-queue-unbounded.md`](./0021-undo-100-offline-queue-unbounded.md) §D2 + §D6 + ADR-0010 | `G-25-QUEUE-INDEXEDDB-ONLY` |
| AT-21-D2-QUOTA-HARD-ERROR | On `QuotaExceededError` from IndexedDB writes, the client MUST surface a hard error banner with copy substantively equivalent to "Local storage full — reconnect to sync, or free space" and MUST pause further mutation enqueuing until either reconnect drains the queue or the user clears storage. Silent `catch + discard` of quota errors is forbidden — the queue's no-data-loss contract (per ADR-0010 + SSOT AT-OQ-05) MUST hold. SHOULD: track usage via `navigator.storage.estimate()` and surface a soft-warning banner at **80%** quota (D5 guidance, not gated). | [`0021-undo-100-offline-queue-unbounded.md`](./0021-undo-100-offline-queue-unbounded.md) §D2 + §D5 | `G-25-QUEUE-NO-SILENT-DROP` |
| AT-21-D3-COMPENSATING-ENQUEUE | An undo action MUST emit a new compensating mutation onto the offline queue — it MUST NOT pop, rewrite, or remove the original mutation entry. The server-visible operational sequence MUST equal the user's local action sequence (do-then-undo arrives at server as two ops, not zero). Optimisations that "cancel" a queued op pre-flush are forbidden — they break LWW conflict resolution per ADR-0010 because field-level timestamps would skip a generation. | [`0021-undo-100-offline-queue-unbounded.md`](./0021-undo-100-offline-queue-unbounded.md) §D3 + ADR-0010 | `G-25-UNDO-COMPENSATING-ENQUEUE` |
| AT-21-D6-MEMORY-RULE-CORRECTION | The `mem://features/editor-core` line 7 text MUST be rewritten from "100-action undo/redo stack. 500-action offline local queue (localStorage) with automatic replay and conflict resolution." to language reflecting D1 + D2 (undo cap 100 in-memory per-tab; offline queue unbounded IndexedDB per ADR-0010 with no `localStorage`). Until rewritten, the memory line is **stale** and AI implementers MUST treat ADR-0021 + ADR-0010 as authoritative. Tracked as a follow-up; does not block this AT-row's gate but blocks F-AUDIT-21 closure. | [`0021-undo-100-offline-queue-unbounded.md`](./0021-undo-100-offline-queue-unbounded.md) §D6 | DOC (reviewer; memory-edit follow-up — no CI gate) |

---

## AT-ADR-G14: ADR-0024 — Ratify 3 soft-confirm triage rulings (#01, #03, #17) — closes F-AUDIT-21

> Ratifies the 4 decision clauses (D1..D4) of [`0024-ratify-soft-confirm-triage-rulings.md`](./0024-ratify-soft-confirm-triage-rulings.md). 5 rows: D2 split into D2-DDL-SINGULAR + D2-ALIAS-BRIDGE-AUTHORITY. Three enforcement-tier rows (D1/D2-singular/D3) reference the `G-24-*` gates already named in the ADR; D2-alias-bridge + D4 are DOC-tier with named future-CI gates. **Closes 7/7 of F-AUDIT-21** — every load-bearing ADR (0015–0017, 0020, 0021, 0023, 0024, 0025) now has machine-checkable acceptance criteria; F-AUDIT-21 is fully discharged.

| # | Criterion | Source | Gate |
|---|-----------|--------|------|
| AT-24-D1-AUDIT-SCORE-FROZEN | The 100/100 spec-completeness audit score MUST NOT be recomputed to absorb post-100 addendums (B1–B4 or any future Bn series). Post-100 work MUST be recorded as a "Related spec-completeness work" footnote on the audit document, never as a new dimension that re-weights the score. The AI Implementability score (this document's footer) is a **separate** metric and is unaffected by this rule — only the original P32 audit score is frozen. Reopening requires a superseding ADR. | [`0024-ratify-soft-confirm-triage-rulings.md`](./0024-ratify-soft-confirm-triage-rulings.md) §D1 | `G-24-AUDIT-SCORE-FROZEN` |
| AT-24-D2-DDL-SINGULAR | All SQLite DDL identifiers (table names, column names, index names, FK names) MUST remain **singular** (`Item`, `Title`, `Owner`, `Tag`, `Mirror`, etc.) per ADR-0001 + ADR-0006. Plural DDL identifiers (`Items`, `Titles`, `Tags`) are forbidden. Renaming any singular DDL identifier to plural requires superseding ADR-0001 **and** ADR-0006 **and** ADR-0024 (triple-lock). The 12 ItemType enum values per ADR-0015 (which are lowercase singular nouns: `task`, `note`, `mirror`, …) are reinforced by this rule. | [`0024-ratify-soft-confirm-triage-rulings.md`](./0024-ratify-soft-confirm-triage-rulings.md) §D2 + ADR-0001 + ADR-0006 | `G-24-DDL-SINGULAR-LOCKED` |
| AT-24-D2-ALIAS-BRIDGE-AUTHORITY | The Spec↔DDL Alias Bridge in `spec/04-database-conventions/00-overview.md` is the **sole** authority for translating spec-prose plural natural-language terms (`Items`, `Content`, `Owners`) to singular DDL identifiers (`Item.Title`, `Item.OwnerId`). Inline ad-hoc translations in feature specs, ADRs, or fixtures are forbidden — they MUST cite the alias-bridge entry. New plural prose terms MUST first be registered in the alias bridge before use elsewhere in `spec/`. | [`0024-ratify-soft-confirm-triage-rulings.md`](./0024-ratify-soft-confirm-triage-rulings.md) §D2 | DOC (reviewer; future CI as `G-24-ALIAS-BRIDGE-AUTHORITY` — markdown grep flags plural→singular translation prose outside the bridge file) |
| AT-24-D3-FAVORITES-TABLE-ONLY | Favorites MUST be implemented as a **table-level** concern — a column or flag (e.g. `Item.IsFavorite BOOLEAN NOT NULL DEFAULT 0`, or a join table `ItemFavorite(ItemId, UserId)`) on the canonical item table. Dedicated shell endpoints named `EP-FAVORITES-LIST`, `EP-FAVORITES-ADD`, `EP-FAVORITES-REMOVE`, or any `/favorites/*` route are forbidden. Listing favorites MUST be a query parameter on the existing item-list endpoint (e.g. `?filter=favorite`), not a sibling endpoint. Any future request to introduce a dedicated `Favorite` table or `EP-FAVORITES-*` shell endpoint MUST first supersede ADR-0001 (per its Worked Example) **and** ADR-0024. | [`0024-ratify-soft-confirm-triage-rulings.md`](./0024-ratify-soft-confirm-triage-rulings.md) §D3 + ADR-0001 | `G-24-FAVORITES-TABLE-ONLY` |
| AT-24-D4-TRIAGE-RECLASSIFICATION | The `.lovable/question-and-ambiguity/00-triage-summary.md` file MUST display a banner at file head re-labelling items #01, #03, #17 from 🟡 **Soft-confirm** to ✅ **Ratified by ADR-0024**. The banner is the authoritative classification; per-item section headers (§§ #01, #03, #17) MAY retain their original 🟡 markers for historical fidelity. The triage file remains the audit trail; this ADR is now the load-bearing source. AI sessions encountering a 🟡 marker on these three items MUST treat ADR-0024 as authoritative and the section marker as historical. | [`0024-ratify-soft-confirm-triage-rulings.md`](./0024-ratify-soft-confirm-triage-rulings.md) §D4 | DOC (reviewer; future CI as `G-24-TRIAGE-BANNER-PRESENT` — grep asserts banner string exists at file head referencing ADR-0024 + items #01, #03, #17) |

---

## F-AUDIT-15 closure note (v1.12.0, 2026-04-29, task #2)

**Finding:** v5 audit (Gemini-2.5-Pro) flagged "scopes over the 15% placeholder-density cap" — concentrated in `36-user-management` (claimed 50% placeholders), `13-cicd-pipeline-workflows`, and other legacy modules.

**Root cause:** Same as F-AUDIT-25 — the pre-#44c gate-#59 placeholder regex matched the bare word "placeholder" in legitimate technical prose (e.g. "Acceptance Summary table…no prose-only claims" + JSON-schema literals containing the word). After the heuristic was tightened (#44c) to require explicit stub markers (`TODO:`, `TBD:`, `FIXME:`, `<!-- STUB`, `Stub Section`, `[PLACEHOLDER]`, `placeholder text`), running the post-#44c density check against the seven scopes v5 flagged yields:

| Scope | Lines | Stub markers | Density |
|---|---:|---:|---:|
| `spec/36-user-management` | 578 | 0 | 0.00% |
| `spec/13-cicd-pipeline-workflows` | 6,472 | 0 | 0.00% |
| `spec/10-powershell-integration` | 2,680 | 0 | 0.00% |
| `spec/14-self-update-app-update` | 3,880 | 0 | 0.00% |
| `spec/16-generic-cli` | 3,838 | 0 | 0.00% |
| `spec/17-generic-update` | 1,946 | 0 | 0.00% |
| `spec/11-research` | 85 | 0 | 0.00% |

**Verdict:** **Zero scopes over the 15% cap.** F-AUDIT-15 is a false-positive cascade of the same parser bug as F-AUDIT-25 and is hereby **CLOSED**.

**Tracking:** v5 honestAssessment warning re placeholder burndown is moot for both F-AUDIT-25 (closed #44c) and F-AUDIT-15 (closed here). v6 audit re-run (task #73) will validate against Gemini-2.5-Pro and is expected to confirm both closures.

**Separate concern (NOT F-AUDIT-15):** Legacy modules `10-powershell-integration`, `13-cicd-pipeline-workflows`, `14-self-update-app-update`, `16-generic-cli`, `17-generic-update` describe a **different product** (browser-extension/Go-binary CLI deploy pipeline) inherited from a prior project. The current scope is the WorkFlowy WP-plugin (resolved 2026-04-25). These modules are content-rich and well-formed but **out-of-scope for the WorkFlowy implementation effort**. Marking them OUT-OF-SCOPE is tracked as a separate task (currently unscheduled — does not affect the AI Implementability score because the score weights only in-scope modules per `mem://preferences/spec-implementability-percentage`).

---

## AT-NS-G15: Spec corpus scoping SSOT (`spec/00-scoping.md`) — closes F-AUDIT-28

> Authored 2026-04-29 to close **F-AUDIT-28** (v6 audit MED, +3 — "Implicit or Out-of-Corpus Scoping"). 4 rows. Two enforcement-tier rows reference the new `G-NS-SCOPING-*` gate IDs reserved in `spec/00-scoping.md` §"Hygiene Gate"; remaining DOC-tier with named future-CI gates per ADR-0031.

| # | Criterion | Source | Gate |
|---|-----------|--------|------|
| AT-NS-SCOPING-SSOT-EXISTS | The file `spec/00-scoping.md` MUST exist and MUST be the sole authoritative source of truth for corpus boundary classification. The file MUST classify every top-level directory under `spec/` and every top-level file under `spec/` (excluding `spec/00-scoping.md` itself) into exactly one of three statuses: `✅ Active`, `📦 Archived`, `🗑 Legacy`. Implicit scoping via memory-only entries, side-channel audit notes, or per-feature-file assumptions is forbidden — they MUST be migrated into rows of this file. | [`../00-scoping.md`](../00-scoping.md) §"Corpus Inventory" | DOC (reviewer; future CI as `G-NS-SCOPING-INVENTORY-COMPLETE` — script diffs filesystem vs inventory tables) |
| AT-NS-SCOPING-STATUS-CLOSED-SET | Each row's Status column in the inventory tables MUST contain exactly one of the three sentinel strings: `✅ Active`, `📦 Archived`, `🗑 Legacy`. New status values, free-text statuses (e.g. "deprecated", "draft", "WIP"), or status-less rows are forbidden. Adding a fourth status requires a superseding ADR that updates §"Classification Vocabulary". | [`../00-scoping.md`](../00-scoping.md) §"Classification Vocabulary" | DOC (reviewer; future CI as `G-NS-SCOPING-STATUS-VALID` — regex match on Status column) |
| AT-NS-SCOPING-LEGACY-NO-ACTIVE-CITATIONS | Files under any directory classified as `🗑 Legacy` (currently `spec/10-powershell-integration/`, `spec/13-cicd-pipeline-workflows/`, `spec/14-self-update-app-update/`, `spec/16-generic-cli/`, `spec/17-generic-update/`) MUST NOT be cited (markdown link, code reference, or AT source-column citation) from any `✅ Active` file. Stale citations from Legacy → Active are permitted (Legacy is read-only). Citations from Active → Legacy MUST be surfaced as content bugs. | [`../00-scoping.md`](../00-scoping.md) §"Hygiene Gate" | DOC (reviewer; future CI as `G-NS-SCOPING-LEGACY-NO-CITATIONS` — markdown grep for Active→Legacy paths) |
| AT-NS-SCOPING-RECLASSIFICATION-VIA-ADR | Reclassifying any scope's status MUST follow the procedure in `spec/00-scoping.md` §"Reclassification Procedure": (1) author a new ADR titled `ADR-NNNN: Reclassify <scope> from <old> to <new>`; (2) the ADR cites this file and the row being changed; (3) the ADR + the inventory edit MUST land in the same PR; (4) the row's `First-classified` column gets a `Reclassified` annotation. Silent reclassifications, memory-only changes, or side-channel notes are forbidden. | [`../00-scoping.md`](../00-scoping.md) §"Reclassification Procedure" | DOC (reviewer; future CI as `G-NS-SCOPING-ADR-FOR-RECLASS` — git-history check that scoping diffs co-occur with new ADR file in same commit/PR) |
