# Error Manage — Acceptance Criteria I/O Fixtures

> **Version:** 0.1.0 (P20 stub seed)
> **Created:** 2026-04-28 (UTC+8)
> **Status:** Stub seed — companion to [`97-acceptance-criteria.md`](./97-acceptance-criteria.md).
> **Format spec:** [`spec/01-spec-authoring-guide/19-acceptance-criteria-io-table.md`](../01-spec-authoring-guide/19-acceptance-criteria-io-table.md)
> **Spawned by:** `.lovable/plans/00-active.md` § P20.

Each row below references one `AT-*` id from the source acceptance file and
restates the binding I/O contract in the SSOT table format. Stubs have a 🟡
marker; replace with concrete fixtures during the next P2 sweep.

---

## `AT-ERRORRESOLUTION-01` — Stub fixture (P20)

| Given | Conditions described in the prose definition of `AT-ERRORRESOLUTION-01` in [`97-acceptance-criteria.md`](./97-acceptance-criteria.md). |
|---|---|
| **When** | The corresponding action / linter / endpoint described for `AT-ERRORRESOLUTION-01` is invoked. |
| **Then** | Observable outcome matches the prose; if a REST envelope is involved, response uses PascalCase `Status` / `Attributes` / `Results` per [`spec/04-database-conventions/06-rest-api-format/`](../04-database-conventions/06-rest-api-format/). |
| **Negative** | The opposite of the documented outcome MUST fail the corresponding test. |
| **Test name** | `at_errorresolution_01` |

> 🟡 **P20 stub.** Replace with concrete commands / JSON request + envelope / file paths during the next P2 sweep. Citation count for this AT in spec/ remains satisfied; this fixture is the binding I/O contract.

## `AT-ERRORRESOLUTION-02` — Stub fixture (P20)

| Given | Conditions described in the prose definition of `AT-ERRORRESOLUTION-02` in [`97-acceptance-criteria.md`](./97-acceptance-criteria.md). |
|---|---|
| **When** | The corresponding action / linter / endpoint described for `AT-ERRORRESOLUTION-02` is invoked. |
| **Then** | Observable outcome matches the prose; if a REST envelope is involved, response uses PascalCase `Status` / `Attributes` / `Results` per [`spec/04-database-conventions/06-rest-api-format/`](../04-database-conventions/06-rest-api-format/). |
| **Negative** | The opposite of the documented outcome MUST fail the corresponding test. |
| **Test name** | `at_errorresolution_02` |

> 🟡 **P20 stub.** Replace with concrete commands / JSON request + envelope / file paths during the next P2 sweep. Citation count for this AT in spec/ remains satisfied; this fixture is the binding I/O contract.

## `AT-ERRORRESOLUTION-03` — Stub fixture (P20)

| Given | Conditions described in the prose definition of `AT-ERRORRESOLUTION-03` in [`97-acceptance-criteria.md`](./97-acceptance-criteria.md). |
|---|---|
| **When** | The corresponding action / linter / endpoint described for `AT-ERRORRESOLUTION-03` is invoked. |
| **Then** | Observable outcome matches the prose; if a REST envelope is involved, response uses PascalCase `Status` / `Attributes` / `Results` per [`spec/04-database-conventions/06-rest-api-format/`](../04-database-conventions/06-rest-api-format/). |
| **Negative** | The opposite of the documented outcome MUST fail the corresponding test. |
| **Test name** | `at_errorresolution_03` |

> 🟡 **P20 stub.** Replace with concrete commands / JSON request + envelope / file paths during the next P2 sweep. Citation count for this AT in spec/ remains satisfied; this fixture is the binding I/O contract.

## `AT-ERRORRESOLUTION-04` — Stub fixture (P20)

| Given | Conditions described in the prose definition of `AT-ERRORRESOLUTION-04` in [`97-acceptance-criteria.md`](./97-acceptance-criteria.md). |
|---|---|
| **When** | The corresponding action / linter / endpoint described for `AT-ERRORRESOLUTION-04` is invoked. |
| **Then** | Observable outcome matches the prose; if a REST envelope is involved, response uses PascalCase `Status` / `Attributes` / `Results` per [`spec/04-database-conventions/06-rest-api-format/`](../04-database-conventions/06-rest-api-format/). |
| **Negative** | The opposite of the documented outcome MUST fail the corresponding test. |
| **Test name** | `at_errorresolution_04` |

> 🟡 **P20 stub.** Replace with concrete commands / JSON request + envelope / file paths during the next P2 sweep. Citation count for this AT in spec/ remains satisfied; this fixture is the binding I/O contract.

## `AT-ERRORRESOLUTION-05` — Stub fixture (P20)

| Given | Conditions described in the prose definition of `AT-ERRORRESOLUTION-05` in [`97-acceptance-criteria.md`](./97-acceptance-criteria.md). |
|---|---|
| **When** | The corresponding action / linter / endpoint described for `AT-ERRORRESOLUTION-05` is invoked. |
| **Then** | Observable outcome matches the prose; if a REST envelope is involved, response uses PascalCase `Status` / `Attributes` / `Results` per [`spec/04-database-conventions/06-rest-api-format/`](../04-database-conventions/06-rest-api-format/). |
| **Negative** | The opposite of the documented outcome MUST fail the corresponding test. |
| **Test name** | `at_errorresolution_05` |

> 🟡 **P20 stub.** Replace with concrete commands / JSON request + envelope / file paths during the next P2 sweep. Citation count for this AT in spec/ remains satisfied; this fixture is the binding I/O contract.

## `AT-ERRORRESOLUTION-06` — Stub fixture (P20)

| Given | Conditions described in the prose definition of `AT-ERRORRESOLUTION-06` in [`97-acceptance-criteria.md`](./97-acceptance-criteria.md). |
|---|---|
| **When** | The corresponding action / linter / endpoint described for `AT-ERRORRESOLUTION-06` is invoked. |
| **Then** | Observable outcome matches the prose; if a REST envelope is involved, response uses PascalCase `Status` / `Attributes` / `Results` per [`spec/04-database-conventions/06-rest-api-format/`](../04-database-conventions/06-rest-api-format/). |
| **Negative** | The opposite of the documented outcome MUST fail the corresponding test. |
| **Test name** | `at_errorresolution_06` |

> 🟡 **P20 stub.** Replace with concrete commands / JSON request + envelope / file paths during the next P2 sweep. Citation count for this AT in spec/ remains satisfied; this fixture is the binding I/O contract.

## `AT-ERRORRESOLUTION-07` — Stub fixture (P20)

| Given | Conditions described in the prose definition of `AT-ERRORRESOLUTION-07` in [`97-acceptance-criteria.md`](./97-acceptance-criteria.md). |
|---|---|
| **When** | The corresponding action / linter / endpoint described for `AT-ERRORRESOLUTION-07` is invoked. |
| **Then** | Observable outcome matches the prose; if a REST envelope is involved, response uses PascalCase `Status` / `Attributes` / `Results` per [`spec/04-database-conventions/06-rest-api-format/`](../04-database-conventions/06-rest-api-format/). |
| **Negative** | The opposite of the documented outcome MUST fail the corresponding test. |
| **Test name** | `at_errorresolution_07` |

> 🟡 **P20 stub.** Replace with concrete commands / JSON request + envelope / file paths during the next P2 sweep. Citation count for this AT in spec/ remains satisfied; this fixture is the binding I/O contract.

## `AT-ERRORRESOLUTION-08` — Stub fixture (P20)

| Given | Conditions described in the prose definition of `AT-ERRORRESOLUTION-08` in [`97-acceptance-criteria.md`](./97-acceptance-criteria.md). |
|---|---|
| **When** | The corresponding action / linter / endpoint described for `AT-ERRORRESOLUTION-08` is invoked. |
| **Then** | Observable outcome matches the prose; if a REST envelope is involved, response uses PascalCase `Status` / `Attributes` / `Results` per [`spec/04-database-conventions/06-rest-api-format/`](../04-database-conventions/06-rest-api-format/). |
| **Negative** | The opposite of the documented outcome MUST fail the corresponding test. |
| **Test name** | `at_errorresolution_08` |

> 🟡 **P20 stub.** Replace with concrete commands / JSON request + envelope / file paths during the next P2 sweep. Citation count for this AT in spec/ remains satisfied; this fixture is the binding I/O contract.

## `AT-ERRORRESOLUTION-09` — Stub fixture (P20)

| Given | Conditions described in the prose definition of `AT-ERRORRESOLUTION-09` in [`97-acceptance-criteria.md`](./97-acceptance-criteria.md). |
|---|---|
| **When** | The corresponding action / linter / endpoint described for `AT-ERRORRESOLUTION-09` is invoked. |
| **Then** | Observable outcome matches the prose; if a REST envelope is involved, response uses PascalCase `Status` / `Attributes` / `Results` per [`spec/04-database-conventions/06-rest-api-format/`](../04-database-conventions/06-rest-api-format/). |
| **Negative** | The opposite of the documented outcome MUST fail the corresponding test. |
| **Test name** | `at_errorresolution_09` |

> 🟡 **P20 stub.** Replace with concrete commands / JSON request + envelope / file paths during the next P2 sweep. Citation count for this AT in spec/ remains satisfied; this fixture is the binding I/O contract.

## `AT-ERRORRESOLUTION-10` — Stub fixture (P20)

| Given | Conditions described in the prose definition of `AT-ERRORRESOLUTION-10` in [`97-acceptance-criteria.md`](./97-acceptance-criteria.md). |
|---|---|
| **When** | The corresponding action / linter / endpoint described for `AT-ERRORRESOLUTION-10` is invoked. |
| **Then** | Observable outcome matches the prose; if a REST envelope is involved, response uses PascalCase `Status` / `Attributes` / `Results` per [`spec/04-database-conventions/06-rest-api-format/`](../04-database-conventions/06-rest-api-format/). |
| **Negative** | The opposite of the documented outcome MUST fail the corresponding test. |
| **Test name** | `at_errorresolution_10` |

> 🟡 **P20 stub.** Replace with concrete commands / JSON request + envelope / file paths during the next P2 sweep. Citation count for this AT in spec/ remains satisfied; this fixture is the binding I/O contract.

---

## Verification

```bash
grep -c "^## \`AT-ERRORRESOLUTION-" spec/03-error-manage/97a-acceptance-criteria-fixtures.md
node scripts/spec-hygiene/00-run-all.mjs
```

## Related

- [`97-acceptance-criteria.md`](./97-acceptance-criteria.md) — Source AT prose
- [`spec/01-spec-authoring-guide/19-acceptance-criteria-io-table.md`](../01-spec-authoring-guide/19-acceptance-criteria-io-table.md) — Format SSOT
